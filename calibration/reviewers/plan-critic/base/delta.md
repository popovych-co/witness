# Delta for auth-refresh (base: empty — first plan)

Both criteria are unrealized: ac-rotate (rotation before expiry, atomic
invalidation) and ac-lockout (5 failed refreshes in 10 minutes lock the
session).
