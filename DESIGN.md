# specflow — Design

A Claude Code framework that takes a change from **brainstorm → PR** through a five-stage pipeline with four evidence-gated transitions. It synthesizes the best of superpowers (obra), mattpocock/skills, specops (Jarvus), and the axi/no-mistakes CLI family (kunchenguid) into one coherent, self-owned tool.

> Status: design revised after **grill #4 — verifiability pass, 2026-07-08** (new rows 55–65, marked ●; amended: 5, 17, 21, 25, 26, 27, 31, 40, 41, 43, 46, 47, 50, 53, 54). Major changes this revision: **reviewer model pin** (`gates.model`, exact ids — the CLI invokes reviewers (48), so it controls the knob; the floor demotes to the fallback chain's last rung), **clean-verdict coverage proof** (verdicts carry required `coverage` anchors — a zero-findings verdict was the one unfalsifiable message; injection twins added to calibration), **adopt re-verification** (adopting a live spec runs the deterministic lane now / flags `unreviewed-amendment`; no debounce — hand-edits aren't flake), **temp-index reviewed sha** (untracked files are part of what reviewers judged), **entry-based round accounting** (cache governs invocation, appends govern rounds — A↔B oscillation trips the bound), **write-validated body templates** (spec: `## Motivation` + `## Behavior`; plan: `## Step: <id>` per manifest step — anchors get guaranteed targets), **stage-skill calibration contracts** (first-try valid-output rate, planted-trap recaps), **canon growth** (`specs/**` glob; re-slice deletes the superseded file — journal keeps lineage), **report-glob merge** (the graduation target is a 20-package pnpm monorepo; both runner modes fixed for it), **write-refusal protocol** (structured errors, 3-attempt bound, journaled), **calibration lifecycle** (`specflow calibrate`, exact ids, gitignored local overlay). Previous revision, same day — external design review (new rows 47–54; amended: 21, 26, 27, 30, 31, 34, 40, 41, marked ✦). Its major changes: **reviewed-content hash** (verdict cache and rounds key on what the gate actually judged — worktree tree-sha at implement/ship, closing a stale-verdict bug), **CLI-witnessed verdicts** (the CLI invokes reviewers headlessly; verdicts are never skill-relayed — the judgment lane gets the same mechanism the write path and red→green already had), **anchor grammar + omission findings**, **calibration model floor**, **canonical content sha** (volatile frontmatter out of every semantic key), **recap amendment** (`recap --amend` — scope can evolve mid-effort), **drift debounce** (fail-closed meets flaky suites), **`specflow adopt`** (hand-edit remediation — the audit gets a fire exit), **plan step schema** (step↔criterion totality, deterministic). Previous revision: grill #3 (rows 36–46, ⟳ marks). Build not started. `BUILD-PLAN.md` **written 2026-07-08** — 4 tracer-bullet slices, CLI-first; licenses audited (see Sourcing). **2026-07-15 amendment (◇):** Decision 66 — UI work TDDs at the browser, end-to-end over the slice (Puppeteer through the repo-owned stack, fakes only at unowned third-party boundaries; motion-side mandate: implement protocol + plan step naming + pr-test lens + calibration seeds). **2026-07-16 amendment (◆):** Decision 67 — bound endgame + evidence recoverability, post-incident c2692b93 (malformed rounds never spend the budget + streak brake; `--revise --upstream` exempt from the bound and resets it; endgame decisions work without a pending run; `next` surfaces bound-stuck gates; evidence judged latest-cycle per tag; filtered junit opt-in with `filter-matched-nothing`; universal `--help`). **2026-07-17 amendment (◈):** grill #5 part 1 of 3 — **repo docs registry** (row 68): `docs:` config map, enumerated keys with shipped consumers only (`conventions` at launch — injected into code-reviewer at implement + ship, fail-closed on missing files), injected doc contents join the lens's verdict-cache key; parts 2–3 (design stage, implement design lens) have scope skeletons in docs/superpowers/plans/. **2026-07-17 amendment (◉):** grill #5 part 2 of 3 — **the design stage** (rows 69–70): a sixth stage between decompose and plan. A `ui`-flagged spec (set at decompose, slicing-critic-policed both ways) in a feature effort earns a **living approved look** — `designs/<spec-id>.html`, a self-contained CLI-written artifact, produced in an interactive `specflow-design` session, judged by the `design-critic` (canon-compliance / spec-coverage / realizability / UX-heuristics, canon injected via `docs.design`), **always stopped for the human**; approval stamps a volatile `design: {sha, spec}` on the spec, and no plan for a ui spec can exist without a matching `design-from` pin. fix/chore ride straight through. Part 3 (implement design lens) has a scope skeleton in docs/superpowers/plans/. **2026-07-18 amendment (❖), grill #5 part 3 of 3 — implement design lens (row 71):** the implement gate can fail a shipped screen. Browser e2e captures (D66) write named PNGs to `.specflow/screens/<plan-id>/` (gitignored, regenerable), `SPECFLOW_SCREENS_DIR`-exported and cleared per evidence cycle; `test-evidence` witnesses a sha per capture; a vision `design-reviewer` joins the feature and fix implement batteries (skipped, journaled, when the plan carries no `design-from` pin) and judges the captures against injected `docs.design` canon + the living `designs/<spec>.html` (both cache-keyed); a UI plan with zero verified captures refuses `screens-matched-nothing`, a doctored capture refuses `capture-sha-mismatch`, before any reviewer runs. Grill #5 complete: no UI feature reaches implement unseen (part 2), no UI diff ships unjudged (part 3). **2026-07-18 amendment (✧), grill #6 — design sight witness (row 72):** being shown a design artifact is now a precondition of gating it. `specflow design <id> --open` is the one place anything is spawned at a human and journals a sha-keyed `design-shown` witness; `gate design` refuses `design-unseen` before invoking any reviewer, `decide design --approve` refuses it too (the gate does not run at the round bound), and `next` routes to `--open` by name. Re-authoring re-arms it. No vision lens at the design gate — the standing stop means a human is present by construction; the implement gate keeps the vision reviewer because it can pass unattended. Scope stated plainly: this witnesses a spawn on the CLI's machine, not perception. Post-incident: benoticed booking-services was authored, committed and gated unseen after the skill's preview step was dropped on a hook block. **2026-07-18 amendment (✸), grill #7 — parallel flows (rows 73–78):** several flows run concurrently in one repo without masking, stalling or silently mis-merging each other. A **flow** is a plan with status `in-progress`; code stays isolated per-plan in worktrees and canon stays shared on the primary root — no new isolation layer, no new state files. `next` becomes a three-tier ladder (advance in-flight → human-owed decisions → start new work) while still answering with exactly one line, so a ship gate's standing stop no longer freezes every other flow; `--flow <plan-id>` addresses one flow as a claim, a worktree cwd infers one as ambient context. The reviewer battery runs outside the global lock (which now covers only the ~100ms write window), and ship fetches and rebases on the true remote tip **before** the battery judges, so no verdict and no human decision is spent on a tree that will never merge. Staleness is solved once rather than three times: gate approval lapses when the reviewed sha moves, generalizing `designPending`'s doctrine that approval is a fact about a sha. Executing the plan surfaced five defects in it, four of them in the ship phase — recorded in rows 75 and 77 rather than quietly fixed. **2026-07-20 amendment (✽), grill #8 — implement-run economics (rows 79–81; amended: 32):** the first full dogfood run on benoticed.co (session d7eb5a85, booking-services) was forensically audited: three implement agents averaged ~510k tokens of context per request and produced 88% of 756M cache-read tokens, 194 of their test runs paid suite width to watch one test, and the only relay trigger was the agent's own judgment arriving at ~500k. The economics become designed: **dispatch budget** (row 79 — `implement.stepsPerDispatch`, default 3; relay only at evidence-cycle boundaries, never mid-red; an early-exit protocol with countable triggers replaces vibes; dispatch arithmetic surfaces at `start` and the plan gate; compaction is the harness's continuity mechanism, not specflow's economics mechanism — relay re-derives from witnessed disk state), **loop width** (row 80 — the inner TDD loop runs the exact test under work, the tagged suite runs only inside the witnessed evidence cycle at step close; fat artifacts go to disposable reader agents), and **dispatch telemetry** (row 81 — a reported, never gate-consumed journal entry per dispatch, so the next cost question is a journal query, not transcript archaeology). **2026-07-22 amendment (✺), grill #9 — session relay + reviewer policy pins (rows 82–83; amended: 4, 10, 45, 79, 80, 81; Open/deferred main-session half resolved):** the second full dogfood run (session c270d08b, booking-services-plan-1) was forensically audited: 32.6h wall against ~11.5h of activity. The engine session babysat Task-subagent dispatches at ~308k average context for 188 opus requests (58M cache-read), dispatch results landed as queued notifications in a session nobody watched, a predecessor's dead dispatch cost 1.7h of inheritance forensics, a 6.4h-wall suspended dispatch held CPU until the e2e evidence run starved past its timeout, and an r1↔r2 reviewer contradiction (the correctness lens banned unavailable-page service fields; the design lens then demanded them back) forced a plan reopen that burned the overnight round. Two structural answers: **session relay** (row 82 — implement and ship execute in fresh human-spawned sessions from a CLI-printed handoff command; Task subagents leave the execution model entirely; the dispatch budget survives as the `/clear` relay boundary), and **reviewer policy pins** (row 83 — `decide --pin` journals content policies; the CLI injects live pins into every subsequent battery; contradicting a pin is a standing stop, not a burned round). **2026-07-29 amendment (✾), post-dogfooding tooling fixes (rows 84–86):** running the pipeline on a second repo surfaced three CLI bugs, one of them silent data loss — `abandon <effort>` destroyed another effort's approved spec amendment because ownership was re-derived with an alphabetical tie-break (row 84); `next` printed an unrunnable `--effort <slug>` placeholder and looped on it forever once the authoring effort was abandoned (row 85); and every chore effort deadlocked at a decompose stage that refuses to gate without specs a chore is forbidden to write (row 86).

---

## North star

Six principles decide every tradeoff below. When torn, favor the earlier one.

1. **State vs motion.** `specs/` describe what must be true (state); `plans/` describe what we're changing (motion). Decompose produces state; plans *derive from* it. Never mixed, never reversed. Specs are **behavior-only** — public surface, never internal interfaces — so refactors don't touch them. This is the spine of the whole design.
2. **Frontmatter is the state.** No state store, no daemon for pipeline position. Every doc self-describes; the CLI derives "where are we" by scanning frontmatter. Committed, git-diffable, resumable. Three kinds of state, never confused: **computed** (derived at scan time, never stored — `blocked`), **stamped** (written by gates — `status`), **cached** (expensive check results stored with an invalidation key — `drift`).
3. **Evidence, then human.** No transition without evidence — deterministic checks + a reviewer verdict. Humans stop where judgment is owed: scope (decompose gate on features), shipping (always), and any blocking finding anywhere. Clean evidence may pass on its own (green path); a human never approves blind, and never rubber-stamps what evidence already settled. Evidence is durable: every check, verdict, and decision lands in the journal.
4. **Clean = single-responsibility.** Each artifact answers exactly one question; no two overlap; one fact has one home; references are one-way. Applied recursively: a fact needed by many specs gets *extracted into its own spec* — this is what killed briefs, stored `blocked`, and multi-parent plans.
5. **Durable where it pays (B-lite).** Spec *depth* is lazy — thin where a slice is trivial, deep where it earns it — but never zero grip: **every spec carries at least one machine-checkable acceptance criterion**. A lying spec is worse than no spec.
6. **Re-entrant everywhere.** Any stage skill, killed at any point and re-run, converges — it detects done-ness from frontmatter plus world state (PR exists, tests pass), never from session memory. Resumability is a testable property, not a hope; the fixture smoke test kills and resumes at every boundary to prove it.

---

## The pipeline

Six stages, five gates. The agent **auto-advances** and stops per the gate policy below. **Stages shrink, never skip** — every stage always runs, scaled to the change; config controls ceremony, never stage existence.

| # | Stage | Produces | Gate |
|---|-------|----------|------|
| 1 | **brainstorm** | *no canon, only evidence* — stateless interview (grill-me style) ending in a **confirmed scope recap** (class + goals / non-goals / constraints with ids + non-binding slice candidates), persisted by `specflow recap` as the birth entry of the **effort journal**; the recap is decompose's input contract and survives session death | none — confirmation is in-conversation; `specflow recap` validates the schema (ids unique, goals non-empty), but no reviewer, no stop |
| 2 | **decompose** | **sliced `specs/*.md`** — new specs and/or amendments to living ones, with `summary`, `depends` (the slice DAG), `covers` (manifest-only goal mapping) and **acceptance criteria in the DSL**; written via `specflow write --effort` | write-time mechanical checks (schema, DAG acyclic, criteria parse, goal ids exist) + **goal-coverage totality**, set-level and deterministic: every goal covered by ≥1 spec, every spec covers ≥1 goal + slicing critic: truly vertical? independently shippable? coverage *quality*, non-goal violations, criteria adequacy, behavior-only, summary accuracy. **Always stops for `feature` class** — scope is approved *as its concrete slicing* |
| 2.5 | **design** | living approved look `designs/<spec-id>.html` for each `ui`-flagged spec (feature only) | **always stops** — the look is human judgment (same footing as ship); `design-critic` verdict + deterministic checks (artifact self-contained, id-attributed, spec ui-flagged) |
| 3 | **plan** | `plans/<id>.md` with mandatory `parent: <spec-id>` and a `derives-from` content pin; steps are **schema'd in the manifest** (`steps: [{id, title, criteria: [ids] \| scaffolding}]`), body elaborates each in a write-validated `## Step: <id>` section; input = spec + CLI-computed delta | write-time mechanical: **step↔criterion totality, both directions** — every step maps to ≥1 criterion or is marked scaffolding, every criterion has ≥1 step (goal-coverage totality, one level down) + plan critic: faithfully derives the **delta**; step *quality* — testable, honest scaffolding marks |
| 4 | **implement** | code + **spec-tagged tests** (CLI-managed worktree per plan; a fresh **session slice** per **≤`implement.stepsPerDispatch` steps** — the dispatch budget, 79 ✽, relayed via `/clear` (82 ✺); TDD) | battery per change class (see Config); exit bar: tagged tests exist and pass **with recorded red→green evidence per added/modified test**, deterministic drift lane green locally |
| 5 | **ship** | PR + green CI | two-lane drift check + final code-review; **always stops** |

**State leads by construction.** A plan cannot exist without the parent spec it implements — whether that spec is newly created or a living spec amended for this change. "Spec leads" is a structural guarantee, not a rule.

**Change classes.** Every effort carries a class — `feature | fix | chore` — **recorded at recap**; decompose reads it from the effort stream. The class sets the ceremony profile, including **per-class reviewer sets** (see Config):
- `feature` — full interview, full slicing, decompose gate stops for scope approval; full reviewer battery. A `ui`-flagged spec earns the design stage before its plan (row 69).
- `fix` — brainstorm shrinks to a two-question interview; decompose routes to the one amended spec (consulting `specflow index`); gates ride the green path with a slimmer battery. **Tripwire:** a `fix` whose manifest *creates* a new spec stops the decompose gate — misroutes usually announce themselves as new specs. **Cold-start note:** on a young canon most fixes legitimately create specs, so this tripwire fires often at first — the dashboard says so explicitly ("canon is young — expect new-spec stops") rather than letting early stops train users that stops are noise. Residual accepted consequence: a *wrong-amendment* misroute is still first caught at the ship gate; the dashboard shows the routing, and autopilot is interruptible. The design stage never arms for `fix` — it rides straight to plan.
- `chore` — motion with **no state change** (deps, tooling): parent = the spec whose implementation area it touches, or `principles.md` for repo-wide work (so `parent: <spec-id | principles>`). **Tripwire:** a chore effort writing any spec content is refused at write time — by definition a class error; reclassify. Principles carry no criteria, so a chore's ship gate has no deterministic drift lane — protection is tests + lint + a one-reviewer battery, and drift is trivially clean by construction. The design stage never arms for `chore` either.

**Multi-slice features.** Decompose emits a **spec DAG** via `depends`. Plans run **sequentially** for now; the DAG already encodes what *could* run in parallel, so drain-the-DAG waves are a later unlock with no data-model change (the write protocol below is already single-writer, so no conflicts either).

**Cross-cutting changes stay per-slice.** A change spanning specs is decomposed as a **sequenced migration** — per-slice amendments ordered via `depends`, expand-contract style (accept both → switch → drop old), each step independently shippable. If the blast radius exists because one fact is written in several specs, decompose first **extracts the shared fact into its own spec** (re-slice via `supersedes`), then amends that one. Plans never get multiple parents.

---

## Artifact model

```
specs/                 state — living canon of vertical slices (B-lite depth)
  principles.md          cross-cutting rules / trade-offs — type: principles
  <slice>.md             one sliced spec: current desired state of a thin end-to-end slice
                         (globbed specs/**/*.md — id lives in frontmatter, not path,
                         so subdirs are free structure when flat hurts)
designs/               state — living approved look per ui-flagged spec (feature efforts)
  <spec-id>.html         self-contained CLI-written mockup; content sha journaled;
                         gate-approved, stamped design:{sha,spec} on the spec
plans/                 motion — each plan derives from exactly one parent spec
  <plan-id>.md           parent: <spec-id> (mandatory)
.specflow/
  journal/<id>.jsonl     evidence — append-only history per artifact AND per effort
                         (effort streams born by `specflow recap`), CLI-written (see Evidence journal)
CLAUDE.md              process: how to operate this repo (native Claude Code file)
specflow.config.yaml   thin config (see below)
```

**Cut, on purpose:** `briefs/`, `CONTEXT.md`, `docs/adr/`.
- **`briefs/`** — a brief's only unique job is capturing scope *before* slicing; the moment decompose passes its gate, that truth lives more precisely in the specs, and a *living* brief becomes a permanently stale second home for it. So brainstorm persists **no canon, only evidence**: it ends with a confirmed scope recap persisted by `specflow recap` as the effort journal's birth entry — append-only history that can't go stale, only be superseded. Scope is approved as its slicing at the decompose gate; the *why* lives in per-spec **Motivation** lines and the decompose commit message. Decompose resumes from the journaled recap after session death, and the dashboard sees in-flight efforts before any spec exists. Remaining accepted cost: a session dying **mid-interview** (before recap confirmation) loses the interview; the ephemeral scratch-dump stays a re-add-on-real-pain option (see Open).
- **`CONTEXT.md` / `adr/`** — their unique jobs don't pay yet; `principles.md` carries standing rules, git/PR history carries the why. Re-add later, append-only, on real pain (term-drift → `CONTEXT.md`; recovering superseded decisions → `adr/`).

### Efforts — the change as a whole

An **effort** is the unit brainstorm→ship runs over: one recap, one decompose, N specs/plans. It is deliberately **not a doc** — no frontmatter, no file in `specs/` — only a journal stream (`.specflow/journal/<effort>.jsonl`) born by `specflow recap`. Set-level facts live there and nowhere else: the confirmed recap, decompose gate-runs, the goal-coverage mapping.

- **State is entirely derived.** *Active* = recap exists, neither complete nor abandoned. *Complete* = computed at scan: decompose gate passed, every produced/amended spec `live` (or `superseded` by a later effort), every child plan terminal. Never stamped — completion is a fact about artifacts, and a stamp could go stale under late abandonment.
- **Scope evolves via `specflow recap --amend <effort>`** — appends a *superseding* recap entry to the same stream (append-only holds; nothing is rewritten). Coverage totality and `covers` validation always read the **latest** recap. An amendment **re-arms the decompose gate's feature-class stop** — scope changed, scope gets re-approved. This is also where revise-upstream lands when decompose-gate findings implicate the scope itself (goals wrong, not slicing wrong): the stop screen offers re-interview → `recap --amend`. Without this verb, the only exit from a mid-effort scope change would be abandoning the whole effort.
- **Slug reuse is refused.** `specflow recap` on a slug whose journal stream already exists — active *or* terminal — refuses and demands a new slug; histories never merge silently.
- **Abandonment is explicit** (`specflow abandon <effort>`), because a walked-away effort is indistinguishable from not-started-yet forever. Terminal entry in the effort journal; landed artifacts walk the per-artifact abandon path with its refusal rules, or the CLI refuses wholesale and names what blocks.
- The dashboard lists active efforts and their class. No timers, no staleness heuristics — a human abandons dead efforts when the listing annoys them.

### Living canon — what specs mean over time

`specs/` always reads as **current desired state**. History lives in git and the journal, not in doc chains.

- **Routine amendment**: decompose edits the spec's content, flips it `live → approved`, and a new plan (same `parent`) realizes the delta. 1:N plans per spec over its lifetime — even within one effort (expand-contract amends a spec twice).
- **`supersedes`** is reserved for **re-slicing** — when boundaries themselves change (split, merge, extraction of a shared fact). The re-slice **deletes the superseded file** in the same state commit: content survives in git, lineage in the frozen journal (`supersedes:` and `--lineage` resolve against **journal** existence); dangling `depends` edges refuse the commit — dependents are rewritten in the re-slice. `specs/` stays literally "current desired state"; `superseded` is a journal-terminal fact, not an on-disk one.
- **Mid-flight amendment — soft lock**: amending a spec whose child plan is in-progress makes `check` warn and the decompose gate demand explicit ack. Drift always measures against *current* content, so the in-flight plan is caught at its ship gate and routed to revise — and a ship-time revise **re-pins the plan's `derives-from`** to the content it now realizes; the pin must always name what was actually verified, or the next `specflow diff` computes its delta from a fiction — so **the CLI stamps the re-pin itself**, from the exact spec content the deterministic drift lane just executed against; a skill-supplied pin would be a reported claim where a witnessed one is available. Honest without blocking course corrections.
- **`live` is a stamped checkpoint**, not cheap derived state: the ship gate stamps it when code demonstrably realizes the content. (Contrast `blocked`, which *is* cheaply derivable and therefore never stored.)
- **Abandonment**: `specflow abandon <plan>` flips the plan to `abandoned` and **selectively reverts the paired amendment state commit — spec/plan paths only, journal paths never** (a full revert would erase evidence; append-only wins), restamping the spec's prior status (`live`, if it was live) and appending the abandonment entry. Deterministic precisely because state commits are small and state-only. Refusals, both handed to the human: a later amendment touched the same spec (newest-first only), or other specs `depends` on a newly-created spec being reverted — a deleted dep would block its dependents forever. The canon must never describe unbuilt intent — the canon forgets, the journal remembers the whole attempted cycle.

### Frontmatter — the single source of truth for position

Every doc carries it. The CLI reads and writes it; nothing else stores pipeline position. The schema itself is versioned once, in config (`schema: 1`) — older repo → warn and point at `specflow migrate` (reserved); newer repo → refuse with "upgrade specflow".

```yaml
# universal
id: auth-refresh                # stable slug — the cross-ref handle (rename via CLI only;
                                # charset [a-z0-9-] write-enforced — ids interpolate into
                                # runner command templates, so the charset is a safety rule)
type: spec | plan | principles
depends: [auth-login]           # INTERNAL DAG: docs that must land first;
                                # persists as the reference graph after landing
                                # (readiness only consults non-live deps —
                                # one field, two phases of one relationship);
                                # record the edge even when the dep is already
                                # live — the reference graph must stay total,
                                # or extraction re-slicing loses its map
needs:                          # EXTERNAL prereqs — tiny check DSL, CLI-evaluable:
  - env: STRIPE_API_KEY         #   env var exists
  - cmd: "npm ls stripe"        #   command exits 0 — confirm-first: new/changed
                                #   cmds prompt once, then join a local
                                #   (gitignored) allowlist; a cloned repo
                                #   never executes silently
  - manual: "Stripe sandbox account created"   # flipped via `specflow satisfy` —
    satisfied: false                           # the write path holds even here
```

Per-type:
- **spec** — `status: draft | approved | live | superseded` (`superseded` is journal-terminal — the re-slice deletes the file); `summary:` — required one-liner (≤120 chars, write-validated): what the slice *is*, where Motivation says *why it exists*; feeds `specflow index` and the dashboard, accuracy policed by the slicing critic; `supersedes: <id>` (re-slicing only); `criteria:` (the acceptance-criteria DSL, below); `drift: {sha, at}` health flag set/cleared by reconcile — status stays `live`, because lifecycle ≠ health; `ui: true` (optional, spec-only, boolean — a slice that changes browser-rendered/interactive behavior; set at decompose, slicing-critic-policed both directions; earns the design stage); `design: {sha, spec}` (**volatile** cached-state stamp — approved artifact sha + the spec content sha it was designed against; written only by the design gate's approve path; scan derives `design-pending` = feature-effort + `ui: true` + missing/stale stamp); body is **template-validated**: `## Motivation` and `## Behavior` headings required at write — doc-gate anchors get guaranteed targets, prose stays free inside.
- **plan** — `status: draft | approved | in-progress | done | abandoned`; `parent: <spec-id>` (**mandatory, singular**; `principles` allowed for chores); `derives-from: <content-sha>` — the exact spec content this plan realizes (canonical content sha — volatile fields excluded, see Evidence journal); `design-from: <artifact-sha>` — **mandatory when the parent spec is `ui`-flagged** (must equal the parent's current `design.sha`; the plan gate re-checks), refused on a non-ui parent; `pr: <n>` set **at PR creation**; body carries one `## Step: <step-id>` section per manifest step (write refuses missing/orphan — step↔body totality).
- **principles** — `status: draft | approved`; exempt from readiness and drift.

**`depends` vs `needs` is the core distinction.** `depends` is the internal readiness graph the CLI walks; `needs` is the external world, every entry machine-checkable or an explicit manual flag. A doc is *ready* only when both are clear. **`blocked` is computed at scan time, never stored.**

### Acceptance criteria — the deterministic lane's contract

Criteria get the same discipline as `needs`: a tiny DSL, every entry machine-runnable. Free prose is not a criterion.

```yaml
criteria:
  - test: "@spec:auth-refresh"   # all tests tagged with this spec id must pass
  - cmd:  "npm run smoke:auth"   # exits 0
```

- **Test tagging.** Implement tags every new test with the parent spec id — **in the test name/title** (`it("rotates token before expiry @spec:auth-refresh")`), never a comment: names are the one string that test filters (`-t`/`-k`), test reports, and grep all see; comment tags would demand a per-framework source→report correlation swamp. The CLI's matcher normalizes case and `-`/`_` so name-hostile languages work (`test_rotates_token__spec_auth_refresh`). Verification is **bidirectional**: every live spec has ≥1 passing tagged test (read from the report); no tag points at a nonexistent spec (source grep, respecting `.gitignore` plus configured excludes — vendored dirs and fixtures would false-positive). Renaming a test never touches the spec — the tag travels with the code, so behavior-only specs (north star 1) survive refactors. A failing test announces which spec it guards; the tag being visible in output is a feature.
- **Execution — two modes, one config key.** `criteria.runner` is either a filtered template (`npm test -- -t "@spec:{id}"` — the fast path for one spec's gate) or `full-suite` + `report: junit:<glob>` (one suite run mapped to all tags — what `check --drift` across the whole canon wants anyway, and the zero-framework-knowledge fallback; the glob matters: recursive monorepo runners emit one report per package, and the CLI **merges** them). The filtered template's contract is explicit: **root-runnable**, args forwarded through recursive runners, passes with zero matching tests (`pnpm -r` + vitest wants `--passWithNoTests`). Multiple runners per repo: deferred with `ship.test`'s singular shape.
- **Command trust.** Every command specflow executes — `criteria.cmd`, `needs.cmd`, and config commands (`ship.test`, `ship.lint`, the runner) — passes **one prompt-once allowlist** (local, gitignored, keyed by command string): first run in a fresh clone confirms the full set, new/changed commands re-prompt. CI opts in explicitly (`SPECFLOW_TRUST_CMDS=1`) — a cloned repo never executes *anything* silently, and a repo's CI trusting the repo's own commands is consent, not a hole (a committed allowlist would add ceremony, not trust: the attacker's PR carries both the command and its entry).
- **Red→green evidence.** A tagged test that *passes* proves existence, not relevance — assertions can rot while the tag stays green forever. `specflow test-evidence` records **CLI-witnessed** runs at the TDD loop's natural red and green points (the CLI executes the runner and observes; the evidence is not skill-reported). The implement gate requires the **fail-before/pass-after pair for every tagged test the plan's diff adds or modifies** — prompt discipline enforced by mechanism. Missing evidence? `specflow verify-red` reconstructs it: stash the diff's non-test files back to base, run (expect red), restore, run (expect green) — safe in the plan's isolated worktree. A "new" test that's green against base code is surfaced as a stop: the test may be vacuous, or the behavior already existed. Pure-`cmd:` criteria and untouched tests need no evidence — the requirement rides the diff, not the spec's history.
- **UI behavior is browser-witnessed, end-to-end.** A plan step that changes user-visible browser behavior TDDs at that surface: its red is a **Puppeteer** test driving the real UI **through the whole slice** — everything the repo owns (frontend, backend, persistence) runs real; fakes are legitimate only at boundaries the repo doesn't own (third-party services), so the test stays runnable in any clean worktree while still proving the slice whole. Headless, tagged and evidence-witnessed like any other test. Puppeteer over runner-shaped tools deliberately — it is a library, so browser tests live inside the single `criteria.runner` the evidence machinery already reaches (multi-runner stays deferred; a browser test *outside* the runner would be structurally unwitnessable — `test-evidence` can't execute it, so the implement gate's red→green requirement refuses the diff anyway). Browser tests own the slice's lifecycle — boot backend + store and serve inside the test, never presume a dev server — or they can't run root-level in a clean worktree. The mandate is **motion-side only**: the implement protocol carries it, plan steps name the browser test, and the pr-test lens treats unit-for-browser substitution — and its mirror, a browser test over a stubbed own-backend — as critical gaps; spec content and the DSL stay untouched — *what kind of test* proves a behavior is HOW, and specs are behavior-only (north star 1).
- **Lifecycle.** Criteria are promises at decompose and obligations at ship: decompose gate → criteria **parse**, adequacy reviewed by the slicing critic (a criterion that can't fail is not a criterion; and a `cmd:` criterion must state its expected behavior in the spec's `## Behavior` section — the template guarantees the home exists (60), the critic polices that the fact lives there; a threshold that lives only inside the script is a fact with a hidden home, and it drifts invisibly); implement exit → tagged tests **exist and pass with red→green evidence** (plus a local run of the full deterministic lane, so ship confirms rather than discovers); ship gate and reconcile → criteria **pass**. Drift-check entries record the per-spec **tag count**, so silent tag attrition (12 tests → 1 across a refactor) trends on the dashboard instead of hiding behind the last green survivor.
- Criteria live in frontmatter, authored by decompose, written by the CLI via the manifest (below).

### Evidence journal — where history lives

`.specflow/journal/<id>.jsonl` — append-only, one file per **artifact** and one per **effort** (set-level facts — the recap, decompose gate-runs, the goal-coverage mapping — live in the effort stream; artifact-level facts in artifact journals; `run_id`/`caused_by` stitch across; one fact, one home). Written only by the CLI, committed with state commits. **Never compacted or rotated — deliberately**: append-only JSONL diffs cleanly and compresses well; immutability is worth more than the disk. **Frontmatter is position; the journal is history.** Without it, "evidence accumulates across rounds" and the 3-round bound would silently reset with every session.

Seven entry types — `recap` / `gate-run` / `human-decision` / `drift-check` / `test-evidence` / `adopt` / `write-refused` (a `recap --amend` appends another `recap` entry — same type, latest wins; `write-refused` is the lightweight authoring-time record — per-violation rules, no content — that lets the dashboard trend first-try valid rate):

```jsonl
{"v":1,"t":"recap","effort":"auth-hardening","class":"feature",
 "goals":[{"id":"g1","text":"Refresh tokens rotate before expiry"}],
 "non_goals":[{"id":"n1","text":"No SSO provider changes"}],"constraints":[],"slices":["token rotation"]}
{"v":1,"t":"gate-run","gate":"plan","artifact":"auth-refresh-plan-2","round":1,
 "run_id":"r-8f2","artifact_sha":"9f3c21e","reviewed_sha":"9f3c21e","specflow":"0.4.1","model":"claude-fable-5",
 "checks":[{"name":"schema","ok":true},{"name":"steps-map-to-criteria","ok":false,"detail":"step 4 unmapped"}],
 "verdict":{"coverage":[{"anchor":"## Context","note":"read"},{"anchor":"## Step 4","note":"see finding"}],
  "findings":[{"blocking":true,"anchor":"## Step 4","claim":"assumes token table exists; AC-2 creates it here"}]},
 "outcome":"stopped"}
{"v":1,"t":"human-decision","gate":"plan","artifact":"auth-refresh-plan-2","round":1,
 "decision":"revise","note":"token table exists in prod; plan against migration 0042"}
{"v":1,"t":"test-evidence","artifact":"auth-refresh-plan-2","phase":"red","runner":"filtered",
 "tests":[{"name":"rotates token before expiry @spec:auth-refresh","ok":false}]}
{"v":1,"t":"drift-check","artifact":"auth-refresh","artifact_sha":"4e7ab90","tags":7,
 "criteria":[{"test":"@spec:auth-refresh","ok":true},{"cmd":"npm run smoke:auth","ok":false}]}
```

Rules that make it survive everything:
- **Content-sha pinning.** Entries reference content by hash — never claims about "current" content, so amendments can't invalidate history, and "has this changed since the verdict?" is one hash compare. Doc content uses the **canonical content sha**: hash of body + non-volatile frontmatter, with the volatile fields (`status`, `drift`, `pr`) excluded — a status stamp or drift flag must never invalidate a verdict, a `derives-from` pin, or a `specflow diff` base (one definition, four consumers: pins, verdict keys, round counting, diff). Code is referenced by **worktree tree-sha**, computed through a **temporary index** (`GIT_INDEX_FILE=<tmp> git add -A && git write-tree`, honoring `.gitignore`, real index untouched) — untracked files are part of what was judged; a protocol test asserts an untracked-only revise changes the sha. Raw `git hash-object` blob shas appear only where a real git object is needed.
- **Ordering is derived, never stored.** Appends travel in state commits; single-writer main makes commit order the total order across all journal files — and because order stays *derived*, the deferred team future re-derives it (topological commit order) instead of migrating stored sequence numbers; storing order "for convenience" would be the regression. `specflow log --all` merges by it. `run_id` stitches entries born of one CLI invocation; `caused_by: {artifact, gate, round}` back-references cross-artifact causality (revise-upstream writes the reopen entry in the parent's journal, linked, never duplicated).
- **Immutable.** `migrate` never rewrites entries; each carries its own `v`. Re-slicing freezes the old spec's journal; `specflow log <id> --lineage` walks the `supersedes` chain. Renames are a CLI verb (`specflow rename <old> <new>`) that moves the journal and rewrites references in one state commit; `check` flags orphan journals.
- **Verdicts are cached state.** A gate invocation whose key — (**reviewed sha**, gate, reviewer-set hash, model, specflow version) — matches a prior gate-run entry **reuses that verdict** — no reviewer re-roll; **the cache governs invocation only**: a reviewed sha equal to the *last* entry's is a resume and appends nothing (the existing entry *is* the evidence), while a different sha appends a new gate-run entry carrying the cached verdict marked `cached: true` — a counted round either way, and the journal already records every key component. **Reviewed sha is per-gate**: canonical content sha of the artifact for the doc gates (decompose, plan); **worktree tree-sha for implement and ship** — the key must bind to what the reviewers actually judged, or an implement revise that changes only code (plan doc untouched) would cache-hit a stale verdict on old code and rounds would never count. **Reviewer-set hash covers prompt *contents*, not names** — lenses are configurable, so a set-membership hash would let an edited lens dodge the key. `--fresh` forces a re-roll for a human who distrusts the cache. Judgment becomes deterministic on re-entry — north star 6 for the verdict lane, not just the deterministic one.
- **The revise hand-off channel.** Revise re-invokes the stage skill with the last verdict + human note — reconstructed from the journal, not session memory, so revise survives session death. Round counter = **gate-run entries since the last approve** (same-sha re-entries append nothing) — revise rewrites the reviewed content (doc or code), so real rounds count themselves; re-rolls and resumes are free, and returning to *previously seen* content still appends (cached verdict, new entry), so A↔B oscillation cannot dodge the bound. A revise that lands **identical content** (reviewed sha unchanged) is surfaced as an explicit *"revise changed nothing"* stop — otherwise the cache would return the same verdict, the round would never count, and the human would loop forever with the bound never tripping. The 3-round bound is real across sessions.
- **Excluded, deliberately:** full reviewer transcripts, full test output (summaries + exit codes; CI run URL at ship), artifact content (shas point into git), decision rationale prose (Motivation lines and commit messages — one fact, one home).

### State-write protocol

**The CLI is the sole writer of `specs/` and `plans/` — all content, not just frontmatter.** Skills author; the CLI writes:

- **Manifest handoff.** A skill emits a body (scratch file) + a structured manifest (id, summary, depends, needs, criteria, covers) and calls `specflow write <id> --effort <e> --meta m.json --body b.md` — the effort hands the CLI the recap to validate against; several efforts can be in flight, so it is never inferred. Validation runs **before** the write — schema, DAG acyclicity, criteria parse, `covers` ids exist in the recap, `summary` present and ≤120 chars, class tripwires (a chore writing spec content → refuse) — so illegal artifacts never exist on disk, and part of each gate's deterministic checking moves to authoring time (gates re-run the checks; they're cheap and idempotent). Refusals are **structured errors** — `{field, rule, got, want}` per violation, built for model self-repair; a stage skill gets **3 write attempts**, then stops for a human with the list (the gate bound's authoring-time twin); every refusal appends a `write-refused` entry to the effort journal, so the dashboard trends **first-try valid rate** — the production mirror of the calibration metric (61).
- **Enforced twice.** Real-time: the plugin ships a **PreToolUse hook** blocking direct Edit/Write on `specs/**` and `plans/**` — whole directories, no region parsing — plus best-effort matching of Bash commands touching those paths; the model gets "use specflow write" as feedback. The hook is **friction; the audit is the guarantee**: state commits carry a `Specflow-State: 1` trailer, and `specflow check` audits that spec/plan diffs appear only in trailer-bearing commits — catching hand-edits, Bash end-runs, and hookless sessions.
- **Audit findings get a mechanical exit: `specflow adopt <path>`.** Re-validates the hand-edited content (schema, criteria parse, class tripwires — the same checks `write` runs), wraps it into a proper trailer-bearing state commit, and appends an `adopt` journal entry; or offers revert. Adopting a **live** spec whose canonical content sha changed runs the deterministic lane on the spot: pass → stays `live`, the entry records the re-verify; fail → `drift` stamps immediately (**no debounce** — a hand-edit is known-changed content, not a flaky suite). A criteria-unchanged prose edit stays `live` with `unreviewed-amendment: true` on the entry, dashboard-surfaced as reconcile work — an adopted edit is never silently pristine. Humans *will* fix typos in their own canon from their own editor — the audit without a fire exit would make the honest path more expensive than the bypass, which is how tools train users to route around them.
- **State always lands main-side, from anywhere.** A CLI invocation inside a linked worktree (implement's gate-runs, test-evidence) detects it and routes state writes through the primary checkout — otherwise implement's own evidence would ride the PR branch, violating code-only branches.
- **One clone, one writer — enforced, not assumed.** `.specflow/lock` (pid + stale detection) serializes state writes across concurrent sessions on the same clone; two terminals on one repo is a Tuesday, not a deferred team scenario.
- **Writes are transactional.** A crash between artifact write and state commit leaves dirty state paths with no trailer commit; the next invocation detects exactly that signature and offers complete-or-rollback — the CLI honoring the same re-entrancy invariant it demands of skills.
- **Commits are path-scoped** (`specs/`, `plans/`, `.specflow/` only); the CLI refuses to commit if those paths are dirty with unrelated changes. Stages 1–3 artifacts are gate-approved on main — the gate *is* their review.
- **PR branches carry code only, never spec/plan changes.** During the PR window the plan correctly reads `in-progress` on main. The ship drift lane reads spec content **from main, never the PR-branch copy** — the branch copy is stale under a mid-flight amendment.
- **Post-merge stamping is lazy.** No daemon: any CLI invocation that scans and finds an in-progress plan with `pr: <n>` queries `gh`; if merged → stamp `plan → done`, `spec → live`, append journal entry, commit. Stale-until-next-scan, always self-healing — the same philosophy as computed `blocked`. Offline or rate-limited, the query times out quietly to "stale, couldn't check" — a scan (and SessionStart's dashboard) never hangs on the network.
- **`sync`** = pull --rebase, then push accumulated state commits. State commits are local-first; pushed at ship or explicit sync. Single-writer by construction — future parallel waves cannot conflict on state.
- *Deferred limitation:* team repos with protected main need state to travel via PR — out of scope until the tool targets teams. (Same for one user on two machines: single-writer holds per-clone; a rejected push is the detection signal.)

---

## Orchestration

**Hybrid hub.** One entry point, always-visible state, one coherent voice across all stages (the axi promise).

- **CLI `specflow`** (deterministic, axi-styled — TOON output, no-arg dashboard, `help:` hints):
  - scan frontmatter → **dashboard** (where you are, the one next action; active efforts and their class; blockedness computed live; drifted specs surfaced as reconcile work; multiple ready specs → it asks rather than guesses)
  - `recap` (validate + persist the scope recap; births the effort journal; `--amend` appends a superseding recap to a live effort) · `next` (run the next stage) · `check` (schema, deps, `needs`, transition legality, `gh` + `claude` presence/auth, trailer audit, orphan tags/journals) · `check --drift [--deep]` (reconcile — see Gates; **read-only in CI**) · `write` (manifest → validated artifact) · `adopt <path>` (re-validate + adopt a hand-edit as a state commit) · `design <spec-id> --file <html> | --reconfirm` (persist the look / re-stamp a no-visual-delta amendment) · `diff <spec>` (delta since last realized state: previous plan's pin → last live stamp → empty for new specs) · `index` (id · summary · status · depends across the **live** canon, grouped by dir — computed, never stored) · `satisfy` (flip a `needs.manual`) · `test-evidence` / `verify-red` (red→green witness — see Acceptance criteria) · `abandon <plan|effort>` · `rename` · `sync` · `clean` (reap stray worktrees) · `calibrate <model>` (tier-2 fixture suite vs a named model → matrix entry; local overlay gitignored, gate-runs it clears stamp `calibration: local`) · `log <id>` (render journal; `--all`, `--lineage`) · `init`
  - **no generation, refined: the CLI never *authors* content** — pure state computation and deterministic ops. Deterministically invoking a pinned reviewer prompt at a gate and capturing its structured output is orchestration, not generation (see Gates) — the distinction that lets verdicts be CLI-witnessed without the CLI ever freehanding a word.
- **Skills** (generative): brainstorm (stateless interview → confirmed recap → `specflow recap`), decompose (routes via `specflow index`), design (`specflow-design` — interactive two-mode look session for `ui`-flagged specs: context → 2–3 distinct directions → converge → self-contained HTML with id-attributed sections; hands the CLI the artifact, never touches `designs/` directly), plan, implement, ship. Each does model work; **skills never touch `specs/` or `plans/`** — they hand the CLI manifests — **and never carry verdicts**: gate reviewers are invoked by the CLI, not spawned or relayed by the stage skill (see Gates).
- **Slash command `/specflow`**: the engine — asks the CLI for the next action, invokes the matching stage skill, loops; stops per gate policy. `--manual` makes every gate stop for this run — caution is a per-session mood, not a repo property, so it's a flag, not config.
- **Hooks**: SessionStart injects the dashboard; PreToolUse guards the write path.

**Autopilot — green path.** The agent drives stage→stage. A gate **auto-passes only when every deterministic check is green AND the verdict has zero blocking findings** — notes don't stop the pipeline; they land in the journal and surface on the dashboard while the artifact is non-terminal (after that, `log` keeps them — the dashboard shows work, not archives). Any blocking finding is a hard stop with full context (`approve · revise · stop`). Standing stops regardless of evidence: the **ship gate always stops**; the **decompose gate stops for `feature`-class changes** (scope is human intent) and for a **`fix` that creates a new spec** (misroute tripwire); the **design gate always stops** for `ui` specs (the look is human intent). The green path needs no flag — it is the default behavior; `--manual` is the only mood switch.

---

## Gates

Every transition is **evidence-then-human**, with the green path as the fast lane:

1. CLI runs the stage's deterministic checks (many already ran at write-time; re-running is cheap and idempotent).
2. **The CLI invokes the reviewer(s) itself** — headless (`claude -p`, never a Task subagent — 82 ✺), vendored prompt, artifact read from disk, structured output enforced — resolved per gate *and change class*. **Verdicts are CLI-witnessed, never skill-relayed** — the exact discipline `test-evidence` already has: the orchestrating agent can neither doctor what reviewers see nor fabricate what they said. The gate lane was the last place where evidence was model-*reported* rather than CLI-*observed*; under autopilot pressure ("work's done, just need the gate green") the relaying agent is precisely the adversary the write path already assumes. `specflow check` probes `claude` presence/auth like `gh`. Reviewers produce a **structured verdict** — findings as `{blocking, anchor, claim}` **plus required `coverage: [{anchor, note}]`** (resolvable anchors over the reviewed content — proof the artifact was actually read; minimum per artifact kind); the CLI validates that every anchor resolves in the reviewed content, so hallucinated findings die mechanically — and a **clean verdict with missing or unresolvable coverage is malformed**: `findings: []` is the easiest verdict to fake (a "report clean" instruction injected via the reviewed content rides one hop past the relaying-agent adversary) and was the only uncheckable one; injection twins in the calibration suite (Decision 56) assert planted defects survive an embedded report-clean instruction. **Anchor grammar is per artifact kind**: doc gates → heading path; code gates → file path + optional symbol, grep-resolved, **never line numbers** (they drift across revisions, and a fail-closed check over brittle anchors would false-stop honest verdicts until stops read as noise). **Omission findings are first-class**: `anchor: {kind: omission, scope: <file | section>}` — scope must resolve; "no timeout anywhere" is the most valuable genre of finding and must be expressible, and a blocking omission stops the gate like any other (never auto-dismissed for anchorlessness). **Fail-closed on malformed verdicts:** a finding whose anchor doesn't resolve rejects the *whole* verdict — never silently pruned (a pruned real finding would let the green path pass on a false clean) — and stops the gate for a human with the malformed verdict attached. Verdicts are **cached state** (see Evidence journal): re-entry on unchanged reviewed content reuses the journaled verdict; `--fresh` re-rolls; entries stamp the **model id** alongside the plugin version, so calibration drift is diagnosable post-hoc. **Reviewer model is pinned** — the CLI invokes reviewers, so it controls the knob: `gates.model` (exact id, aliases refused), default = the matrix's best; fallback chain **pinned → next available calibrated id → session default + floor warning**. **Model floor** (the chain's last rung): a gate-run on a model outside the shipped calibration matrix (Decision 41) is flagged in the journal entry and warned loudly on the dashboard (`--manual` treats it as a stop) — calibration binds nothing if the model is one nobody measured; `specflow calibrate` regenerates the matrix per model, and a self-calibrated (local-overlay) floor stamps `calibration: local` on every gate-run it clears. Reviewer prompts are calibrated to one bit: *blocking = you would block a merge over this* — and that calibration is regression-tested against planted-defect fixtures (Decision 41). (Lenses configurable per gate; defaults vendored in; the verdict-cache key hashes lens *contents*, so a customized lens is a different reviewer.)
3. Green (all checks pass, zero blocking findings) → auto-pass, unless this gate is a standing stop. Otherwise: dashboard shows checks + verdict + artifact; human chooses `approve · revise · stop`. Notes append to the journal either way.

**Revise semantics.** `revise` re-invokes the *same* stage skill with the reviewer verdict + the human's note attached — reconstructed from the journal, so it survives session death; the artifact updates in place (via `specflow write`) and the gate re-runs. Evidence accumulates in the journal across rounds. Hard bound: **3 rounds per gate** (rounds = **gate-run entries since the last approve** — same-sha re-entries append nothing, so re-rolls and resumes are free; identical content is an explicit "changed nothing" stop; *previously seen* content re-appends with its cached verdict and counts, so A↔B oscillation trips the bound — see Evidence journal), then the gate refuses to loop and forces a human decision (approve-with-override or stop). When findings implicate an earlier artifact — code faithful, plan wrong — the stop screen offers **revise upstream**, reopening the parent artifact's stage with a `caused_by` link in both journals; from the decompose gate, upstream means the scope itself — re-interview via `recap --amend` (Decision 52).

**Two-lane drift check (ship gate).**
- **Deterministic lane (fail-closed):** the CLI executes the parent spec's criteria — tagged tests and commands, read from main. Unit-testable, reproducible, blocks on failure.
- **Reviewer lane (advisory):** a headless reviewer judges semantic drift between prose and code; blocking findings stop the gate for human judgment, never silently block.

Because every plan has a parent spec, there is no "slice with no spec" case to slip through.

**Design gate.** Reviewed = the artifact (`designs/<spec-id>.html`) + the parent spec; standing stop always (same footing as ship — the look is human judgment, not evidence-gated). Approval (via `decide --approve`) stamps `design: {sha, spec}` onto the spec and journals a `design-stamp` entry. Revise-upstream splits by cause: slicing wrong → reopens the effort's decompose; scope wrong → `recap --amend`.

**Sight witness (design gate).** A standing stop is only human judgment if the human was shown the thing. Sight is a **precondition of gating**, not a byproduct of it: `specflow design <id> --open` spawns the platform opener on `designs/<spec-id>.html` and journals a `design-shown` entry carrying the artifact's sha, the opener command it resolved, and the `user@host` it ran on. `gate design` refuses **`design-unseen`** from `resolve()` — before any reviewer is invoked, so the refusal is free — and `decide design --approve` refuses the same way, because the gate does not run at the round bound and approve is the act that stamps. `next` routes a registered-but-unshown artifact to `--open` by name. Sha-keying is what survives revise rounds: re-authoring invalidates prior sight exactly as re-capturing invalidates a witnessed screenshot (Decision 71). The act lives in the CLI, not the skill, for the same reason verdicts do (Decision 48) — but the CLI *refuses without it* rather than performing it during the gate. Gate-time spawning was designed and rejected: `runGate` calls `resolve()` before four short-circuits that write no entry (resume, changed-nothing, bound, malformed-streak), so the window would open on paths the journal never learns about — including the case where a human re-runs the gate after fixing their opener, reads the artifact it opens, and is then told by the replayed entry that nobody has seen it. Refusing keeps the effect and the record at one site. The principle the incident actually teaches is *silent versus loud*, not *who acts*: an agent that drops the show step now hits a loud refusal its repair loop already handles. **What this witnesses is a spawn on the machine the CLI ran on, not perception** — over SSH or in a container it shows nobody anything, and `SPECFLOW_OPENER` can be pointed at a no-op. That is why the resolved command and host are on the record: a degenerate opener reads as a degenerate opener in the journal instead of being blessed by a flag named "suppressed". **The design gate gets no vision lens.** Presentation is judged by the human here (the gate always stops, so a human is present by construction) and by the vision `design-reviewer` at implement (which can pass unattended, so nobody is). `design-critic` stays a **source** lens — coverage, canon, realizability; it is not blind to presentation by defect, it is scoped away from it by design.

**Design lens (implement gate).** When a plan pins a `design-from` design (part 2 — the plan is UI-work), the implement battery adds a **vision `design-reviewer`** over the browser e2e's witnessed screenshots. The CLI exports `SPECFLOW_SCREENS_DIR` to the runner during `test-evidence` and clears it per cycle, so captures are latest-cycle by construction; each PNG's sha is journaled on the `test-evidence` entry (the same CLI-witnessed trust chain as red→green — a hand-swapped screenshot refuses `capture-sha-mismatch`). The reviewer reads the captures by path (multimodal `claude -p`, Read tool) and judges them against the injected `docs.design` canon and the living `designs/<spec>.html` — the approved *direction*, never pixel equality — with generic UX heuristics as the baseline where the repo has no canon. Anchors are capture filenames; a blocking finding stops the gate like any other. A UI plan with zero verified captures refuses `screens-matched-nothing` before a reviewer is invoked; a feature/fix plan that is not UI-work drops the lens, journaled `skipped` (stages shrink, never skip). The living design's contents hash into `prompts_sha`, so an amended design re-rolls a cached verdict on unchanged code.

**Reconcile — out-of-band changes.** Commits that bypass the pipeline (human hotfixes) rot specs silently between ship gates. `specflow check --drift` runs the deterministic lane across every live spec at any time — cheap, CI-schedulable, **but CI runs it read-only**: nonzero exit + report, never a journal append or flag write (CI as a second writer would break single-writer main); the next *local* run stamps — stale-until-scan, the lazy-stamp philosophy again, with CI's own red status as the notification channel in between. `--deep` adds the reviewer lane for a chosen spec. **The `drift` flag is debounced**: every local run journals its `drift-check` entry, but the spec's `drift: {sha, at}` flag (status stays `live` — lifecycle ≠ health) is stamped only after **two consecutive failing local runs** — a single red surfaces on the dashboard as "seen red once, unconfirmed", not a state commit. Fail-closed lanes meet flaky suites in the real world; a flapping flag is commit churn that trains users to ignore red, which is how deterministic lanes die. The flag clears on the next passing run. The dashboard trends flap frequency from drift-check history — a criterion that keeps flapping is itself reconcile work (fix the test or the spec). Per-criterion `retries:` is deferred to real pain.

---

## Ship

**Ship is a skill; the CLI does the deterministic steps.** The skill orchestrates: run tests → run lint → deterministic drift lane → `gh` PR create (records `pr:` in the plan) → **mechanical rebase if main moved → watch CI** (rebase before the watch, or the watched run is stale) → always stop for the human. On a real (semantic) conflict the CLI reports and hands control back to the skill — the agent resolves, then re-invokes the CLI to continue. The CLI's "no generation" definition stays exactly true. Ship is re-entrant: killed mid-watch, a re-run resumes from `pr:` + world state.

**Worktrees are CLI-managed.** Created at implement start (path recorded in the journal), reaped when the plan reaches a terminal state — the lazy stamp noticing the merge, or `abandon` (branch ref kept for archaeology, worktree deleted). `specflow clean` sweeps strays.

**No orchestrator binary; `gh` and `claude` are declared requirements** — `gh` for PR operations, `claude` for CLI-invoked gate reviewers (Decision 48). `specflow check` probes presence + auth for both; if absent, the affected step stops loudly with help text (ship at PR creation; any gate at reviewer invocation).

---

## Sourcing

**Vendor + adapt.** Copy strong, close-fit skills in as raw material, rewrite them to fit our spine and vocabulary, depend on none at runtime. Reimplement only the glue (orchestration, decompose, CLI). Bootstrap loop: copy vendor text → **you mark polish targets** → grind into "our rules."

**License-gated.** Vendoring = derivative work. Verify each source's license *before* copying a line. Any restrictive one flips that piece to clean-reimplement.

**Audit (2026-07-08):** superpowers (obra) **MIT** · mattpocock/skills **MIT** · kunchenguid gh-axi / chrome-devtools-axi / no-mistakes **MIT** · pr-review-toolkit (Anthropic, claude-plugins-official) **Apache-2.0** · **JarvusInnovations/specops — NO LICENSE (all rights reserved)**. Consequence: specops is **concept-inspiration only, zero text copying** — the decompose row below was already "new", now it's a hard rule. Vendored MIT/Apache text keeps upstream copyright notices in a `LICENSES/` dir + per-file attribution headers; Apache-2.0 additionally requires stating changes.

Vendor map (initial):

| specflow piece | vendored from |
|---|---|
| brainstorm | mattpocock `grill-me` (interview style) + superpowers `brainstorming` — **stateless; ends in a confirmed scope recap, writes no doc** |
| decompose | new (specops `specs/` model — slice into specs; expand-contract sequencing; extraction re-slice; inspired by `to-issues` minus the tracker) |
| plan | superpowers `writing-plans` (derive the how from a parent spec + delta) |
| implement | superpowers `test-driven-development` + mattpocock `tdd` (near-duplicate — merge the union) + spec-tagging discipline |
| ship | new (no-mistakes step sequence, re-owned; skill + CLI split) |
| gate reviewers | pr-review-toolkit `code-reviewer`, `silent-failure-hunter`, `type-design`, `pr-test` — **vendored in**, so gates work on a bare install and outside prompt updates can't silently change gate behavior |
| slicing critic + plan critic + drift reviewer | **new** — no close-fit source; written against the recap, coverage, and delta contracts. Slicing-critic lenses: slice quality, coverage quality, non-goal violations, criteria adequacy, behavior-only, summary accuracy, depends-refs, ui-flag honesty |
| design (skill) | benoticed `redesign-method.md` (process, concept-adapted, zero text) + Anthropic `frontend-design` (greenfield direction, Apache-2.0) — **two-mode: new-screen / amend-living-look**; `design-critic` lens is new material |

---

## Deliverable & distribution

- **Claude Code plugin** (skills + commands + `/specflow` + SessionStart & PreToolUse hooks) — the primary UX.
- **Thin CLI** `specflow` — **Node/TS, run via `npx`** (matches the axi family; zero-install; one toolchain). Deterministic ops only. **The plugin pins the exact CLI version** (`npx specflow@x.y.z`) — plugin and CLI release as one unit, so skill↔CLI skew is impossible by construction (the same refuse-on-newer discipline `schema: 1` applies to repos).
- **Config**: thin, convention-first. Strong defaults; expose only what earns it. Everything else (paths, worktree location, CI polling) is convention.

```yaml
# specflow.config.yaml — the whole surface
schema: 1                    # frontmatter schema version (repo-wide)
# paths: { specs: specs, plans: plans, designs: designs }   # canon + design-artifact roots (defaults shown; must be distinct, non-nested)
gates:
  model: claude-fable-5      # reviewer model pin — exact id, aliases refused;
                             # fallback: next calibrated id → session default + floor warning
  decompose: { reviewers: [slicing-critic] }
  plan:      { reviewers: [plan-critic] }
  implement:
    reviewers:               # per change class; a plain list = all classes
      feature: [code-reviewer, silent-failure-hunter, type-design, pr-test, design-reviewer]
      fix:     [code-reviewer, silent-failure-hunter, design-reviewer]
      chore:   [code-reviewer]
  ship:      { reviewers: [drift-reviewer, code-reviewer] }
docs:                        # repo docs registry — a key ships only with its consumer; unknown keys refused
  conventions: [docs/code/architecture.md]   # → code-reviewer at implement + ship; contents join the verdict-cache key
  design: [docs/ui/design-language.md]       # → design-critic at design + design-reviewer at implement (cache-keyed)
criteria:
  runner: 'npm test -- -t "@spec:{id}"'   # filtered mode (root-runnable contract); or: runner: full-suite + report: junit:**/reports/junit.xml (glob — CLI merges)
ship: { test: "npm test", lint: "npm run lint", branch: main }
# no autopilot key — the green path is the behavior; `--manual` is a per-run flag
```

---

## Decision log

The rationale trail (ADR-lite; captured here since `adr/` is cut). Rows changed in grill #4 (2026-07-08, verifiability pass) are marked ● (latest mark wins); untouched review-revision changes keep ✦, untouched grill-#3 changes keep ⟳. Grill-#3 rows started at 36; review rows at 47; grill-#4 rows at 55. Row 66 (◇) added 2026-07-15 — post-build amendment, no grill. Row 67 (◆) added 2026-07-16 — post-incident amendment, no grill. Row 68 (◈) is grill #5 part 1, 2026-07-17. Rows 69–70 (◉) are grill #5 part 2, 2026-07-17. Row 71 (❖) is grill #5 part 3, 2026-07-18. Row 72 (✧) is grill #6, 2026-07-18. Rows 73–78 (✸) are grill #7 — parallel flows, 2026-07-18. Rows 84–86 (✾) added 2026-07-29 — post-dogfooding tooling fixes, no grill. Row 87 (❂) is grill #10 — multi-harness support, 2026-07-30 (its table row is absent below; row 88 restates the four residuals it settled). Row 88 (✹) is grill #11 — harness-routed reviewers, 2026-07-31.

| # | Decision | Choice | Why |
|---|----------|--------|-----|
| 1 | Deliverable | plugin + thin axi-styled CLI | native to Claude Code; determinism where the model shouldn't freehand |
| 2 ⟳ | Pipeline | 5 stages / 4 gates: stateless brainstorm → decompose → plan → implement → ship; briefs cut; **brainstorm persists no canon, only evidence** (`specflow recap` → effort journal birth) | scope truth lives in specs the moment slicing is approved; a *living* brief is a stale second home — an append-only recap entry can't go stale, only be superseded |
| 3 | Sourcing | vendor + adapt, mark-polish, license-gated | "own solution" from battle-tested raw material |
| 4 ⟳ | State | frontmatter is state; **CLI is the sole writer of `specs/` and `plans/` (manifest handoff)**; state writes main-side **even from worktrees** (routed via primary checkout); PR branches code-only; **lazy post-merge stamping**; `.specflow/lock` serializes concurrent sessions; writes transactional (dirty-without-trailer detected → complete/rollback) | one source of truth that's also branch-coherent; validation before write; stale-until-scan is self-healing, like computed `blocked`; single-writer is mechanism at every layer, including against the CLI's own crashes |
| 5 ● | Spec model | **living canon**: specs always read as current desired state; amendments edit in place; `supersedes` = re-slicing only, and the re-slice **deletes the superseded file** (62) | only model consistent with "specs describe what must be true"; history belongs to git + journal — which is exactly why the dead file can go |
| 6 | Orchestration | hybrid hub | always-know-where-you-are; one coherent voice |
| 7 ⟳ | Gates | evidence-then-human; green path = deterministic green + **zero blocking findings** (notes don't stop; dashboard shows them only while the artifact is non-terminal); standing stops: ship always, decompose for features and for fix-creating-new-spec | reviewers always emit *something* — one calibrated bit (blocking?) keeps autopilot reachable without rubber-stamping |
| 8 | Durable docs | `specs/` + `plans/` + native `CLAUDE.md`; cut briefs, CONTEXT.md, ADR | clean = single-responsibility; don't pay for permanence that doesn't earn it |
| 9 | Ship | ship is a skill; CLI runs deterministic steps; `pr:` recorded at PR creation; **rebase before CI watch**; semantic conflicts hand back to agent; `gh` declared + probed (offline → quiet "stale, couldn't check", never a hang) | keeps "CLI = no generation" exactly true; a rebase after the watch invalidates the watched run |
| 10 ⟳ | Implement | sequential; a fresh implementer **session** (82 ✺) + **CLI-managed worktree** per plan (reaped at done/abandoned; `clean` sweeps strays); TDD; exit = tagged tests pass **with red→green evidence** + local drift lane green | clean context per plan, real isolation, DAG-parallel-ready; ship confirms rather than discovers |
| 11 | Validation target | tool-first, validate on a real project — which repo stays deliberately open | realistic UX signal before generalizing; no locked decision couples to the choice |
| 12 | Vocabulary | spec = the slice (state); plan = its implementation (motion); specs are behavior-only (public surface) | one term per concept; refactors must not touch state docs |
| 13 ⟳ | Reviewers | configurable per gate **and per change class** (defaults: feature ×4 · fix ×2 · chore ×1; plain list = all classes); defaults vendored in (pr-review-toolkit, license-gated); **slicing-critic and plan-critic are new, not vendored** | gates work on bare install; no silent drift from upstream prompt edits; gate cost must shrink with the change or users route around the tool |
| 14 | Config surface | thin, convention-first | minimal to learn / keep honest |
| 15 | Spec ↔ code | two-lane drift: deterministic lane **executes the criteria DSL** (tagged tests + cmds, read from main), fail-closed + reviewer prose lane, advisory | fail-closed needs something falsifiable to close on; prose judgment belongs to reviewers |
| 16 | CLI runtime | Node/TS via `npx`; **plugin pins the exact CLI version** | matches axi family; plugin + CLI release as one unit — skew impossible by construction |
| 17 ● | Status enums | split per type — spec: draft→approved→live→superseded (`superseded` journal-terminal — the file is deleted at re-slice, 62); plan: draft→approved→in-progress→done\|abandoned; `blocked` computed, never stored | doc lifecycle ≠ pipeline position; `done` is motion vocabulary and doesn't belong on state docs |
| 18 | Mid-flight amendment | soft lock: warn + gate ack; drift always against current content | honest without blocking course corrections; stale-pin would ship code matching old truth |
| 19 ⟳ | Ceremony scaling | stages shrink, never skip + change classes `feature\|fix\|chore` **recorded at recap** (decompose reads the effort stream); fix routing consults `specflow index` | routing (new spec vs amend which) is model work — a mechanical bypass can't do it under living canon; the class is a property of the effort, not an argument |
| 20 ⟳ | Command trust | tiny `needs` DSL: `env` / `cmd` / `manual+satisfied` (flipped via `specflow satisfy`); **one prompt-once allowlist covers every command source** — `needs.cmd`, `criteria.cmd`, config commands (local, gitignored); CI opts in via `SPECFLOW_TRUST_CMDS=1` | readiness must be CLI-computable; a cloned repo must never execute *anything* silently; CI trusting its own repo is consent — a committed allowlist adds ceremony, not trust |
| 21 ● | Revise | same-stage rerun with verdict + note attached — **reconstructed from the journal**, so it survives session death; max 3 rounds (**gate-run entries** since last approve — same-sha appends collapse, previously-seen content re-appends cached and counts (59)) then forced human decision; a revise landing identical content → explicit "changed nothing" stop; revise upstream with `caused_by` links, reaching brainstorm via `recap --amend` (52) | bounded, auditable, re-entrant; misdirected blame has an exit; rounds count revision attempts, not invocations; a no-op revise must be visible — and oscillation must count — or the bound never trips |
| 22 | Cross-cutting motion | singular `parent` forever; sequenced per-slice amendments (expand-contract) ordered via `depends`; duplicated facts → extract shared spec | "atomic" cross-slice changes are migration sequences in disguise; duplication is the real smell |
| 23 | Schema versioning | `schema: 1` in config; warn on older, refuse on newer; `migrate` reserved (never rewrites journals) | frontmatter in user repos is a public API |
| 24 | Field hygiene | `parent` everywhere (no `implements`); `owner` cut until a consumer exists; `type: principles` (draft→approved, exempt from readiness + drift) | one term per concept; don't store fields nothing reads |
| 25 ● | Regression safety | **two tiers.** Protocol tests, per-commit: recorded manifests replayed through the real CLI — gates' own deterministic checks as assertions, **kill/resume at every stage boundary** (now including brainstorm→decompose, via the journaled recap), no model in the loop. Calibration tests, nightly + pre-release + on model change: real skills against the committed fixture repo, **under per-skill contracts (61)** | per-commit model runs = cost + flake + ignored reds; prose *content* never asserted — but the **blocking bit on planted-defect fixtures is asserted statistically** (41); the gate checks ARE the assertions — zero new machinery; stage skills assert first-try valid rate (61) |
| 26 ● | Evidence journal | committed `.specflow/journal/<id>.jsonl`, append-only, CLI-written, one file per artifact **and per effort**; entries: **recap** / gate-run / human-decision / drift-check / **test-evidence** / **adopt** / **write-refused** (64), pinned to **canonical content shas** (docs) and **tree-shas** (code), stamped with plugin version **and model id**; `run_id` + `caused_by` links; **never compacted** | frontmatter is position, journal is history; the round counter has no other session-surviving home; verdicts stay traceable to the exact content, reviewer version, and **model** that produced them; accepted cost of never-rewrite: readers support every historical entry `v` forever |
| 27 ● | Verdict schema | structured findings `{blocking, anchor, claim}` + **required `coverage: [{anchor, note}]` — a clean verdict must prove reading (56)**; **per-kind anchor grammar** (docs: heading path; code: file + optional symbol, never line numbers) + **omission findings** (`anchor: {kind: omission, scope}` — scope must resolve, blocking omissions stop like any finding); CLI verifies anchors resolve — **an unresolvable anchor rejects the whole verdict, fail-closed** (stop for human, never prune); green = zero blocking; notes → journal; **verdicts are cached state** — key (**reviewed sha** · gate · **reviewer-prompt contents** · model · specflow version), cache-hit re-entry appends nothing, `--fresh` re-rolls | "zero findings" over free prose is a parsing judgment call; anchors kill hallucinated findings mechanically — but need a grammar, or fail-closed false-stops honest verdicts (line numbers drift) and the most valuable findings (omissions) become inexpressible; a silently pruned finding would green-light a false clean; caching makes judgment re-entrant (north star 6); a zero-findings verdict was the one unfalsifiable message — coverage anchors close it (56) |
| 28 ⟳ | Criteria DSL | `test:` (spec-id tags **in test names**, normalized matcher) + `cmd:`; execution via `criteria.runner` — **filtered template or full-suite + report parse**; bidirectional verification (report-side pass, grep-side existence with excludes); lifecycle parse → exist+pass+**red→green evidence** → pass | names are the one string filters, reports, and grep all see — comment tags need a per-framework correlation swamp; tags survive renames, protecting behavior-only specs (12) |
| 29 ⟳ | Abandonment | `specflow abandon <plan\|effort>` **selectively reverts** the amendment commit — spec/plan paths only, **journal never** — restamps prior status, appends the abandonment entry; refuses over stacked amendments **or waiting dependents** | canon must never describe unbuilt intent — but the journal must remember the attempt; a full revert would erase evidence; a deleted dep would block dependents forever |
| 30 ✦ | Plan delta | plan input = full spec + `specflow diff` (base: previous plan's pin → last live → empty) + prior plans; plan records `derives-from: <content-sha>` (canonical, 51); **ship-time revise re-pins it** to the content actually verified — **stamped by the CLI** from what the drift lane executed against, never skill-supplied | a plan realizes a delta, not a state; the pin disambiguates stacked amendments and is for delta + traceability, never grading; a stale pin would compute the next delta from a fiction; a reported pin where a witnessed one is available would be the provenance hole (48) in miniature |
| 31 ● | Write-path enforcement | PreToolUse hook blocks Edit/Write on `specs/**` `plans/**` wholesale + best-effort Bash matching; `Specflow-State: 1` commit trailer audited by `check`; audit findings exit via **`specflow adopt`** (re-validate → trailer commit + `adopt` entry; live + changed sha → deterministic lane runs now, prose-only → `unreviewed-amendment` flag (57)) or revert | single-writer must be mechanism, not prompt discipline; **hook = friction, audit = guarantee** — the audit catches what the hook can't (hand-edits, Bash end-runs, hookless sessions); an audit without a mechanical exit trains humans to bypass — honest path must stay cheapest |
| 32 | Re-entrancy | north-star invariant: any stage, killed anywhere, converges from frontmatter + world state, never session memory; smoke-tested by kill/resume | resumability is a testable property, not a hope; ship's CI watch is the forcing case |
| 33 | Drift marker | health flag `drift: {sha, at}` on the spec; status stays `live`; state taxonomy named: computed / stamped / cached | lifecycle ≠ health; drift is too expensive to recompute per scan (unlike `blocked`) and too stateful for a status enum |
| 34 ✦ | Scope recap | brainstorm ends with a confirmed, **schema'd** recap — class + goals/non-goals/constraints **with ids** (`g1`/`n1`/`c1`) + non-binding slice candidates — persisted by `specflow recap` as effort-journal evidence; **`--amend` appends a superseding recap** (latest wins; see 52); slug reuse refused | decompose consumes a confirmed contract that survives session death; checks and verdicts anchor to ids instead of paraphrase; structure carries identity, not meaning — no canon written, evidence can't go stale, and scope evolution is an append, not a rewrite |
| 35 | Autopilot surface | no config key; `--manual` per-invocation flag stops every gate | caution is a per-session mood, not a repo property; expose only what earns it |
| 36 | Effort identity | committed effort journal stream `.specflow/journal/<effort>.jsonl`, born by `specflow recap`; set-level facts (recap, decompose gate-runs, coverage mapping) live there and nowhere else; effort is **not a doc** — no frontmatter, no file in `specs/` | the decompose gate reviews a *set*, which no single artifact owns; duplicating set-level entries per spec would break one-fact-one-home and make the round counter ambiguous; north star 6 becomes true at the first boundary |
| 37 | Recap contract | schema at the verb: effort slug, class, `goals`/`non-goals`/`constraints` as `{id, text}`, slices as plain strings; validation = parse, unique ids, goals non-empty; free prose inside items | structure carries identity (anchors, coverage), prose carries meaning; more structure would let the recap compete with specs for scope truth |
| 38 | Goal coverage | `covers: [g-ids]` per decompose manifest; write-time: ids exist in the recap; gate-time: **bidirectional totality** — every goal covered, every spec covers ≥1 goal; mapping recorded in the effort journal, **never spec frontmatter** | coverage *existence* becomes deterministic (the critic judges *quality*); the orphan-spec direction is a mechanical scope-creep tripwire; goal ids are effort-scoped — frontmatter carrying them would be a stale second home |
| 39 | Effort lifecycle | completion **computed** at scan (decompose passed + specs live/superseded + plans terminal); abandonment **explicit** (`specflow abandon <effort>`: terminal journal entry, walks per-artifact abandons under their refusal rules); no timers | completion is a fact about artifacts — a stamp could go stale under late abandonment; a walked-away effort is not derivable from anything, so it needs a human verb |
| 40 ● | Verdict caching | verdicts are **cached state**: key (**reviewed sha** — per-gate, see 47 — gate, **reviewer-prompt-contents hash**, model, specflow version); cache governs **invocation** — appends follow sha-vs-last-entry, previously-seen content re-appends `cached: true` and counts (59); `--fresh` re-rolls; gate-runs stamp the model id + reviewed sha | the judgment lane must be re-entrant like everything else; the journal already records every key component — pure read-side logic, no new state; model stamping makes calibration drift diagnosable; keying on the *stage artifact* instead of the *reviewed content* was a stale-verdict bug at implement/ship (47) |
| 41 ● | Calibration fixtures | tier-2 suite (nightly, pre-release, on model change): planted-defect artifacts + known-good twins **+ injection twins (embedded "report clean" — the planted defect must still be flagged, 56)** per reviewer; assert the **blocking bit** statistically (~10 seeds, ≥9/10 catch, ≥9/10 clean); red blocks release; results ship as a **per-model calibration matrix** that the runtime pin + floor (55, 50) read, regenerated by `specflow calibrate` (65); stage skills carry their own contracts (61) | vendoring froze the prompts but not the model under them; a reviewer that can't flag a planted egregious defect is a decorative gate — and nothing else in the system would ever reveal that; calibration that doesn't bind the runtime is a report, not a guarantee |
| 42 | CI read-only | CI drift checks report via exit code only — never a journal append or flag write; the next local run stamps | CI as a second writer breaks single-writer main; stale-until-local-scan is the lazy-stamp philosophy applied once more; CI's red status is itself the notification |
| 43 ● | Criteria runner | tag in the **test name**; matcher normalizes case and `-`/`_`; `criteria.runner` = filtered template (`-t "@spec:{id}"`, root-runnable contract) **or** `full-suite` + report **glob** (CLI merges per-package reports, 63); grep-side existence honors `.gitignore` + excludes; multi-runner deferred | the adapter was the hardest unbuilt deterministic piece and it was invisible; names collapse filtering, reporting, and grepping into one convention — everything else is a swamp |
| 44 | Red→green evidence | `specflow test-evidence` records CLI-witnessed red/green at TDD's natural points; implement gate requires the pair per **diff-added/modified** tagged test; `specflow verify-red` reconstructs missing evidence (stash non-test files → red, restore → green); green-against-base = vacuous-test stop; drift entries trend per-spec tag counts | a passing tagged test proves existence, not relevance; TDD already produces the proof — witness it or lose it; discipline enforced by gate check, not prompt hope |
| 45 | Class-scaled review | per-class reviewer sets (defaults ×4/×2/×1); tripwires: `fix` creating a new spec → decompose gate stops; `chore` writing spec content → write-time refusal | a one-line chore paying a 4-reviewer battery trains users to bypass the tool; misroutes announce themselves mechanically — catch them where they're cheap |
| 46 ● | Canon navigation | required spec `summary:` (≤120 chars, write-validated; *what it is* vs Motivation's *why*); `specflow index` = id · summary · status · depends across the **live** canon (`specs/**` glob), grouped by dir, **computed, never stored** (62); fix routing must consult it; critic polices summary accuracy | routing is model work over a canon that only grows; a computed index is navigability without a second home; no `find` verb — the model already has grep and `specs/` is read-open |
| 47 ● | Reviewed-content hash | every gate defines its **reviewed object**: doc gates (decompose, plan) → canonical content sha of the artifact; implement + ship → **worktree tree-sha** via temp index (`add -A` + `write-tree` — untracked included, 58); verdict cache **and** round counter key on `reviewed_sha`, journaled per gate-run | the stage artifact (plan doc) and the reviewed content (code) diverge at implement — keying on the plan blob would cache-hit a stale verdict after a code-only revise and rounds would never count at exactly the gate with the most rounds |
| 48 | Verdict provenance | **the CLI invokes reviewers itself** — headless (`claude -p`), vendored prompt, artifact read from disk, structured output enforced; verdicts CLI-witnessed, never spawned or relayed by stage skills; `claude` probed by `check` like `gh`; "no generation" refined to "never *authors*" (pinned-prompt invocation + capture = orchestration) | the judgment lane was the last skill-*reported* evidence path in a design whose spine is CLI-*witnessed* mechanism (write trailer audit, red→green, allowlist); the relaying agent under autopilot pressure is the same adversary the write path already assumes |
| 49 | Anchor grammar | per artifact kind: docs → heading path; code → file + optional symbol (grep-resolved), **never line numbers**; omission findings first-class (`{kind: omission, scope}`, scope must resolve, blocking omissions stop normally) | fail-closed anchor validation without a grammar either false-stops honest verdicts (line drift) until stops read as noise, or degrades to substring-matching that kills nothing; omissions — "no timeout anywhere" — are the most valuable finding genre and were structurally inexpressible |
| 50 ● | Model floor | plugin ships the per-model calibration matrix (41); **reviewer model pinned first (55)** — the floor guards the chain's last rung: a gate-run on an uncalibrated / below-floor model → flagged `gate-run` entry + loud dashboard warning; `--manual` treats it as a stop; never a silent pass | the *session* model is user-chosen, but the CLI invokes reviewers (48) and pins *theirs* (55) — the floor covers what the chain can't reach; a reviewer battery at 3/10 planted-defect catch is decorative, and post-hoc model stamping alone only explains bad merges after they land |
| 51 | Canonical content sha | doc references hash **body + non-volatile frontmatter** — `status`, `drift`, `pr` excluded; used by `derives-from` pins, verdict keys, round counting, `specflow diff`; code = tree-sha; raw blob shas only where a git object is required | volatile stamps (live-stamp, drift flag, pr number) must not invalidate semantic references — else every stamp churns caches and every diff drags frontmatter noise; one definition, four consumers |
| 52 | Recap amendment | `specflow recap --amend <effort>` appends a **superseding** recap entry to the same stream; coverage totality + `covers` validate against the **latest** recap; amendment **re-arms the decompose gate's feature stop**; decompose-gate revise-upstream lands here; slug reuse refused (active or terminal) | scope evolves mid-effort and coverage was frozen against the birth recap — the only exit was abandoning the whole effort; append-only holds (nothing rewritten, latest wins); re-approval is owed because scope is human intent (north star 3) |
| 53 ● | Drift debounce | `drift` flag stamps after **2 consecutive failing local runs**; every run journals its `drift-check` entry regardless; single red = dashboard "unconfirmed", no state commit; flag clears on next pass; dashboard trends flap frequency; per-criterion `retries:` deferred; **adopt-triggered lane failures stamp immediately** — known-changed content is not flake (57) | real suites are stochastic — fail-closed without debounce means a flapping flag, commit churn, and humans trained to ignore red, which is how deterministic lanes die; the journal keeps the full record either way |
| 54 ● | Plan step schema | plan manifest carries `steps: [{id, title, criteria: [ids] \| scaffolding: true}]`; write-time **step↔criterion totality both ways** (every step maps or is scaffolding; every criterion has ≥1 step); body elaborates each step in a write-validated `## Step: <id>` section (60); critic judges step *quality* only | "steps-map-to-criteria" was a deterministic check named over unstructured prose — model judgment in a deterministic costume; structure carries identity, prose carries meaning (37), goal-coverage totality one level down (38) |
| 55 ● | Reviewer model pin | `gates.model` — exact id, aliases refused; default = the matrix's best; fallback chain **pinned → next available calibrated id → session default + floor warning (50)** | 48 handed the CLI the knob (`claude -p --model`) — floor-only was solving the pre-48 problem; a warning users can't act on trains them to ignore warnings; aliases re-point under the calibration |
| 56 ● | Clean-verdict proof | verdict schema requires `coverage: [{anchor, note}]` — resolvable anchors over the reviewed content; zero findings + missing/unresolvable coverage = malformed → fail-closed stop; calibration gains **injection twins** (embedded "report clean" instruction; planted defect must still be flagged) | anchor validation only checked findings that *exist* — `findings: []`, the verdict autopilot wants, was the one unfalsifiable message; injection rides content one hop past 48's adversary, and a faked clean auto-passes the green path |
| 57 ● | Adopt semantics | adopting a live spec with a changed canonical sha runs the deterministic lane **now**: pass → `live` + re-verify entry; fail → `drift` stamped, **no debounce**; criteria-unchanged prose edit → `live` + `unreviewed-amendment: true`, dashboard reconcile work | `live` means a gate witnessed code realize the content — adopt made it assert that about content no gate saw; a hand-edit is known-changed content, not flake, so 53's debounce doesn't apply; the fire exit keeps stamp discipline |
| 58 ● | Reviewed-tree fidelity | implement/ship reviewed sha = tree-sha of a **temp index** (`GIT_INDEX_FILE=<tmp> git add -A && git write-tree`, `.gitignore` honored, real index untouched); protocol test: untracked-only revise ⇒ new sha | `write-tree` hashes the index — an unstaged new test file left the sha unchanged: stale cache-hit + uncounted round, 47's bug reintroduced through the index |
| 59 ● | Round accounting | cache governs reviewer **invocation** only; appends follow **sha-vs-last-entry** — same sha = resume (no append), different sha = new gate-run entry (cached verdict marked `cached: true`); rounds = entries since last approve | distinct-sha counting froze at 2 under A↔B oscillation: the cache-hit appended nothing and "changed nothing" never fired (sha differed vs last) — the bound structurally couldn't trip at exactly the loop it exists to break |
| 60 ● | Body templates | write-validated, structure-only: spec body = `## Motivation` + `## Behavior`; plan body = one `## Step: <id>` per manifest step, missing/orphan refused; prose free inside sections | heading-path anchors (49) had no guaranteed targets; the `cmd:`-threshold home rule was unenforceable prose; step↔body totality is 54 one level down — structure carries identity, prose carries meaning (37) |
| 61 ● | Stage-skill calibration | per-skill tier-2 contracts (~10 seeds): decompose ≥9/10 first-try valid (write checks + coverage totality) **+ planted-trap recaps** (non-goal bait, duplicated-fact extraction); plan ≥9/10 (step totality); implement 3 seeds (tags + red→green evidence); brainstorm exempt (interactive — the recap schema gates at the verb); metric = **first-try valid-output rate** | 41 closed the decorative-reviewer hole; stage skills had the same hole — a degraded skill surfaces as silent write-refusal friction that nothing measured; 64 gives the metric its production mirror |
| 62 ● | Canon growth | CLI globs `specs/**/*.md` — id is frontmatter, not path; subdirs are free user structure, index groups by dir; re-slice **deletes the superseded file** in the same state commit (journal frozen = lineage; `supersedes:`/`--lineage` resolve against journal existence; dangling `depends` refuse — dependents rewritten in the re-slice); index lists live canon only | flat-dir plus accumulating superseded files degrade routing (46) and violate "current desired state" literally; deletion is safe precisely because history's homes (git, journal) already exist — `superseded` becomes journal-terminal |
| 63 ● | Runner reality | `report:` takes a **glob**; the CLI merges junit reports; filtered-template contract explicit (root-runnable, args forward through recursive runners, passes with zero matches); graduation precondition: both modes verified on benoticed.co; multi-runner stays deferred | the graduation target is a 20-package pnpm monorepo with no root workspace — both specced modes failed on it as written; "real pain later" was scheduled for graduation day one; report-merge is trivial deterministic code and config stays singular |
| 64 ● | Write-refusal protocol | refusals return structured `{field, rule, got, want}` lists; stage skills get **3 write attempts** then stop-for-human with the list; each refusal appends a lightweight `write-refused` effort-journal entry; dashboard trends first-try valid rate | the repair loop was unbounded and its errors prose — token burn invisible, degraded skills indistinguishable from healthy ones; 3 attempts mirrors the gate bound (21); the trend is 61's metric in production |
| 65 ● | Calibration lifecycle | matrix entries + pins are exact model ids; `specflow calibrate <model>` runs the tier-2 suite and emits a matrix entry (maintainer release = run + publish); users self-calibrate into a **gitignored local overlay** — gate-runs a local floor clears stamp `calibration: local` | a shipped matrix is a wasting asset: model releases and retirements make staleness the default state, silently reopening the hole 50 closed; regeneration must be one command, and a user-side escape hatch must be visible in evidence, never silent |
| 66 ◇ | UI-work TDD | implement protocol: a step changing browser-rendered/interactive behavior reds with an **end-to-end Puppeteer** test driving the real UI through the slice's real stack (headless, self-hosting the whole slice — repo-owned backend and store run real, fakes only at unowned third-party boundaries; living in the criteria runner's suite); plan steps name that test; pr-test lens gains both substitution gaps — unit-for-browser and stubbed-own-backend; pr-test calibration gains a seed per substitution mode; DSL + spec content untouched | user-visible behavior must be proven at the surface the user sees, over the slice it actually spans — markup-string unit tests keep the tag green through real UI breakage, and a browser test over a canned backend keeps it green while the slice is broken end-to-end; ownership draws the fake line (third-party fakes keep CI runnable, own-stack fakes gut the proof); Puppeteer is a library, not a runner, so browser tests ride the existing single-runner evidence machinery (multi-runner stays deferred) and `test-evidence` witnesses their red→green unchanged; the HOW lives motion-side — specs stay behavior-only (north star 1) |
| 67 ◆ | Bound endgame + evidence recoverability | (a) rounds count only substantive runs — a `malformed` outcome is the battery's failure and never spends the human's budget; two consecutive malformed rounds on an unchanged model+prompts refuse further runs with a remedy (`--fresh` overrides); (b) `--revise --upstream` is exempt from the bound and **resets** the target gate's budget; (c) at the bound `decide` accepts `--stop` / `--approve --override` / `--revise --upstream` without a pending run, anchored to the last gate-run; the gate's bound short-circuit, the bound refusals, and the post-run help enumerate every live exit incl. `abandon`; `next` surfaces bound-stuck gates as decisions before any motion; render shows `round N of 3`; (d) evidence is judged latest-cycle per tag: the newest red is the verdict and a green must post-date it — one vacuous red no longer poisons a tag forever; (e) filtered runners may declare `criteria.report: junit:<glob>`; with a report configured (either mode), zero tag-matched tests refuse `filter-matched-nothing` before journaling; `--help` answered centrally for every verb | incident c2692b93 (benoticed, booking-services-plan-1): one vacuous red permanently poisoned the evidence check under `.some()` over the append-only journal, a pr-test malformed round burned the terminal bound round, and after `--stop` the gate (bound short-circuit appends nothing) and decide (nothing-pending requires a run) pointed at each other in a livelock whose only exit was `abandon` — the human left the pipeline and hand-edited the worktree; a bound that punishes tool failure and blocks its own escape valve exactly when three failed rounds prove the plan wrong inverts the design's intent (21: the bound stops churn, not recovery); evaluation-side evidence fixes preserve journal immutability (north star 3) |
| 68 ◈ | Repo docs registry | `docs:` config map — **enumerated keys with shipped consumers only** (`conventions` at launch; unknown keys refused, naming the shipped set); values = non-empty repo-relative file lists (paths-family validation, `.specflow/` reserved); consumer wiring: `conventions` → code-reviewer at implement + ship, injected as a `## Repo conventions` block ahead of the reviewed content; **injected doc contents join `prompts_sha`** — an edited doc is a different reviewer (cache miss, live re-roll), the rule lens contents already follow; a configured file missing at gate time refuses fail-closed before any journal append; `specflow check` probes every configured path; the `design` key lands with the design stage (grill #5 parts 2–3) | reviewers already inhale the consumer repo's CLAUDE.md ambiently (`claude -p` runs in-repo) — but ambient context is unpinned (a canon edit cache-hits stale verdicts), unfocused (every reviewer pays for everything), and unreachable by stages that don't run reviewers in-repo; an explicit registry makes injection selective and cache-keyed; no-key-without-consumer keeps thin config honest (a dead key is a config lie); fail-closed missing-file mirrors the write path — silently skipping a promised canon would green-light reviews the config claimed were anchored |
| 69 ◉ | Design stage | a sixth stage between decompose and plan: each `ui`-flagged spec (feature only; flag set at decompose, slicing-critic-policed both ways) earns a living `designs/<spec-id>.html` — self-contained, CLI-written via `specflow design`, spec-scoped, re-slice deletes it with the spec; produced in an interactive `specflow-design` session (context → 2–3 directions → converge → self-contained HTML with id-attributed sections); reviewed by the `design-critic` (canon-compliance / spec-coverage / realizability / UX-heuristics — canon injected via `docs.design`, blocking when present, UX-baseline-blocking when absent); **always stops** (same footing as ship); approval stamps **volatile** `design: {sha, spec}`; scan derives `design-pending` (feature + ui + missing/stale stamp); `--reconfirm` clears a no-visual-delta amendment without a session; revise-upstream → decompose (slicing) / recap --amend (scope) | incident c2692b93: a D67-clean pipeline still ships an ugly screen — design quality was judged by no stage, no reviewer, no lane, only the human after ship. The look is approved *before code* exactly as scope is approved before slicing (decompose's standing stop). Specs stay behavior-only (north star 1): the flag is a boolean, the look lives in `designs/`, the stamp is volatile — *what a screen looks like* is HOW; `design-from` makes the prerequisite structural, not a rule |
| 70 ◉ | Design pin + reviewed kind | plan frontmatter `design-from: <artifact-sha>` — mandatory for ui parents (= parent's current `design.sha`, design not pending), refused otherwise; write + plan gate both enforce; verdict gains a `design` reviewed-kind (anchors: `design#<element-id>` for the artifact, `<spec-id> > ## Heading` for the spec; coverage must prove **both** read); the design gate injects `docs.design` into the critic via the part-1 registry (fail-closed); `approveMeta` on the gate spec stamps the non-status `design` field at human-approve time (the gate always stops, so the stamp rides `decide --approve`, not a passed run) | the plan realizes an approved look, not a fresh invention — a stale/absent pin computes motion from a fiction (mirrors `derives-from`, Decision 30); the reviewed-kind gives the critic a real anchor namespace so hallucinated findings die mechanically (49); `approveMeta` generalizes the stamp path so an always-stopping gate can still write state on approval, reusing decide's txn |
| 71 ❖ | Implement design lens | vision `design-reviewer` in the **feature + fix** implement batteries, active only when the plan pins a `design-from` design (else dropped, journaled `skipped` — chore excluded, an accepted residual); reviewed content = the browser e2e's witnessed screenshots — `test-evidence` exports `SPECFLOW_SCREENS_DIR`, clears it per cycle (latest-cycle by construction), journals a **sha per capture** (CLI-witnessed like red→green); captures gitignored + worktree-excluded (absent from tree-sha and diff — regenerable, never committed); gate refuses **`screens-matched-nothing`** (pinned plan, zero verified captures — the D67 filter-matched-nothing pattern) and **`capture-sha-mismatch`** (on-disk bytes ≠ witnessed sha) before any reviewer runs; the lens judges canon compliance (injected `docs.design`, blocking), gross divergence from the living `designs/<spec>.html` (blocking — approved *direction*, not pixels), spec-behavior visibility, and UX heuristics (baseline-blocking without canon); anchors are capture filenames (verdict.ts `screens` reviewed-kind); the living design injects into the lens's docs → hashes into `prompts_sha`, so an amended design re-rolls on unchanged code; calibration gains a `screens` suite (invisible active state, buried action, field-wall, missing behavior + injection twin) | the incident (benoticed booking-services) shipped a canon-violating form because no stage looked at the rendered screen — D66 proves behavior at the browser, and a Puppeteer test is exactly as happy with an ugly form as a good one; the design must be judged where the user sees it, over the slice it spans, against the repo's own written canon + the human-approved look; witnessed shas close the doctoring hole the relaying agent opens under autopilot pressure (48); gitignored-but-regenerable keeps evidence recoverable without committing binaries (D67's recoverability principle); skip-not-omit keeps thin plans honest (45) |
| 72 ✧ | Design sight witness | Being shown a design artifact is a precondition of gating it. `specflow design <id> --open` is the sole spawn site: it opens `designs/<spec-id>.html` and journals `design-shown` with the artifact sha, the resolved opener command, and `user@host`. `gate design` refuses **`design-unseen`** from `resolve()` before any reviewer runs; `decide design --approve` refuses the same, because the gate does not run at the round bound and approve is what stamps; `next` routes registered-but-unshown to `--open`. Sha-keyed, so re-authoring re-arms it. `--open` is reachable for any registered artifact regardless of ui/feature eligibility — gating it there would dead-end the refusals that point at it. `SPECFLOW_OPENER` overrides the binary (bare name or path) and is recorded on the entry; there is no suppression flag, because there is nothing to suppress once the gate does not spawn. **No vision lens at this gate** — presentation is the human's here (standing stop = a human present by construction) and the vision `design-reviewer`'s at implement (which can pass unattended), leaving `design-critic` a source lens by scope, not by defect; `design-critic` gains a non-blocking both-directions coverage check (name sections the spec does not promise); every skill's ground rules gain "a refused or hook-blocked command is a stop, not a step to drop" | the benoticed booking-services incident (2026-07-18): a 34-frame artifact was authored, registered, committed and gated **without the human ever being shown it** — they had approved a *direction* from a throwaway comparison page, then were shown a critic's findings *about* the design in place of the design. D69's standing stop assumed sight; nothing witnessed it. The skill's preview step was dropped when a hook blocked the compound command that carried it — the same relaying-agent adversary (48) the write path and gate lane already assume, one layer down. The lesson is *silent versus loud*, not *who performs the act*: a dropped preview is silent, a refused gate is loud and costs nothing (it fires before a reviewer is invoked) and the skill's 3-attempt repair loop already handles it. Having the gate spawn the opener itself was designed and rejected — `resolve()` runs before four `runGate` short-circuits that write no entry, so it would open windows the journal never records, including the contradiction of telling a human nobody has seen what they are looking at. Registering unseen is harmless (no stamp, no pin, revertible); *gating and approving* unseen is the defect, so the checks sit at gate and decide, not at register. Sha-keying reuses D71's witnessed-capture discipline. Scope is stated honestly: this witnesses a spawn on the CLI's own machine, not perception — hence the opener command and host on the record instead of a "suppressed" bypass | |
| 73 ✸ | Flow unit + addressing | a **flow** is a plan with status `in-progress` — it begins at `specflow start` and ends when the merge stamp flips it to `done`. Deliberately **never a stored field**: it lives in the `--flow <plan-id>` flag, the `flows` dashboard block, and prose (`lane` is taken by drift/judgment lanes, `run` by `run_id`/`gate-run`, `plan` by the stage name). `next` stays a one-line oracle — output contract unchanged (`next:` / `stage:` / `target:` / `note:`) — and with several flows actionable picks **most-advanced-first** (ship > implement gate > implement > worktree-missing), ties broken by id. `next --flow <id>` is a **claim**: refuses `unknown-flow`, `not-started` (draft *or* approved — deliberately not `start.ts`'s `not-approved`, whose predicate differs), `terminal-status`. A worktree cwd is **ambient**: it scopes the read-only `next` and *degrades* to the global ladder rather than refusing. `flowAction(root, cfg, plan)` is the single derivation — `--flow`, tier 1 and the dashboard all consume it, never a re-derived shorthand | a `next` that returns a list makes the human a participant in every iteration and collides with the documented no-progress stop; one line in, one line out keeps the driving loop stateless. Most-advanced-first is WIP-limiting, not cosmetic — it drains toward merge instead of fanning out, which is what keeps concurrent rebases (and therefore lapsed ship gates, 75) rare. Claim-vs-ambient is the write path's split one layer out: an explicit id may select a target for a mutation, an inferred directory may only scope a question — which is why inference lives on `next` alone, every mutating verb taking its id explicitly from `target:`. A shorthand like `pr ? 'ship' : 'implement'` is wrong in three of the five states a flow occupies, unacceptable on the surface whose job is orientation |
| 74 ✸ | Three-tier ladder | `computeNext` stops being a flat first-match ladder: the soundness rungs (`pendingTxn`, canon errors) stay on top, then **tier 1** advance something already in flight, **tier 2** human-owed decisions and bound endgames, **tier 3** start new work. A flow blocked on its *own* pending decision or round bound is **not** tier-1 motion (`flowBlocked`). Waiting decisions ride in `note:` on every turn and in the dashboard's `gates` block between sessions. Single-flow behavior is unchanged — the property a dedicated test pins | ship carries `standingStop` unconditionally and the gate forces `outcome: stopped` whenever it is set, so **every flow that reaches ship guarantees a pending decision**; under the flat ladder that decision returned a `decide` line, which the driving loop routes to END YOUR TURN — so the moment any flow reached ship, every other flow stopped advancing. That is the terminal state of every flow, not a corner case. Tier 3 *below* tier 2 keeps the trade honest: "don't idle while in-flight work can move" is right, "start a brand-new plan rather than let the human decide on a finished one" is not. The blocked-flow filter is not an optimization — without it a stopped flow monopolizes tier 1 and starves tier 2 of the very decision that unblocks it |
| 75 ✸ | Gate approval lapses on content change | `gateSettled(entries, gate, currentSha?)` — a gate is settled only while the verdict that settled it still describes the current content; `reviewed_sha` ≠ current sha re-arms it. The check sits **above both settling branches**, so a human `--approve --override` lapses too. Scoped to tree-reviewed gates (implement, ship); `design` already lapses via `designPending`; plan/decompose deferred to real pain. `shipPhase` gains a gate-return edge — a PR existing no longer short-circuits to `watch` | approval was permanent regardless of what happened to the reviewed content afterward: edit a worktree after an approved implement gate and you ship code no gate ever read, and re-running `specflow ship` was a one-command bypass of every check below it. Concurrency does not create the hole; a rebase on every sibling merge makes it constant. `designPending` (D69) already established the doctrine — approval is a fact *about a sha*, not a permanent fact — so this generalizes an existing mechanism instead of adding a third: the earlier shape carried a write-time staleness refusal, a bespoke ship re-arm *and* a file-overlap detector, all attacking the same thing. **Execution finding:** `worktreeTreeSha` covers the whole worktree — `plans/` and `.specflow/journal/`, not only code — so any state commit that reaches the worktree lapses the gate. That is what makes 77 load-bearing rather than tidy: ship's own `pr #N` stamp advanced main, the watch-phase rebase pulled it into the worktree, and approve → pr → lapse → gate livelocked |
| 76 ✸ | Reviewer battery outside the lock | the repo-global lock wraps only the **write window** — journal append, stamps, state commit — not the multi-lens LLM battery. `entries` (which feeds `appendKind`, `boundReached` and the round number) is **re-read inside** the lock and the bound re-checked there, because the journal may have moved while the battery ran. Content staleness is deliberately *not* re-checked at write time | the battery held a repo-global lock for minutes on a lock whose real job protects a ~100ms commit, so every state-writing verb in every other flow refused for its full duration — the single biggest blocker to parallelism. Moving it leaves no currently-protected write unprotected: the battery writes nothing (every symbol is an in-memory local; reviewer caching is journal-derived via `appendKind`, not an on-disk cache). Without the re-read, two concurrent gate runs on one target both journal round N and both slip past the bound. The write-time staleness check is omitted on purpose — the entry records the sha it judged and is therefore honest; 75 stops a stale verdict *settling* its gate at consumption time, which is the cheaper and more general place. Proved by a fakebin lock probe asserting an **exact** probe count: five gate paths invoke zero reviewers, so `> 0` would pass vacuously on all five |
| 77 ✸ | Ship rebases before it reviews | `rebaseIfMoved(wt, shipBranch, remote = 'origin')` resolves its base from the **fetched remote tip** and returns a `Result`, refusing `no-remote` / `fetch-failed` / `textual-conflict` / `push-failed` rather than degrading. It runs in the **gate** phase, ahead of the battery; `awaiting-decision` touches nothing; `watch` never mutates. `--autostash`, because implement deliberately leaves the worktree uncommitted and ship owns the sole code commit. The post-rebase push fires only where an upstream exists — the gate now precedes `createPr`, so a first ship has none. `baseMoved` is a **separate `shipPhase` input** from `treeSha`, and an approved-but-stale-base plan routes back to the gate instead of opening a PR on a stale base. `textual-conflict` exits FINDINGS (a hand-back); every other rule exits REFUSED | nothing in `src/` ever fetched, so with flows merging concurrently local `<shipBranch>` lagged the true tip: the ancestry check reported `clean` against a provably-behind base and the conflict resurfaced on GitHub after CI had gone green. Placement compounded it — the rebase ran *after* the gate, the human decision and the PR, so a battery and a human were both spent on a tree without any sibling's merged work, and only then did the rebase produce the tree that would actually merge. A configured remote that will not fetch is not the offline case: degrading to the local ref there would silently rebase on a stale base immediately before a merge, the exact failure this exists to prevent. `awaiting-decision` stays untouched because a human reading `decide ship --show` is reading findings pinned to a sha — moving the tree under them would lapse (75) the very gate they are deciding. **Execution finding:** this state machine was the least-modeled part of the design — four of its five steps carried a defect, and the tree-sha/base-moved livelock was invisible until 75 and 77 both existed |
| 78 ✸ | State commits bypass host hooks | `commitWithTrailer` passes `--no-verify`. Ship's code commit does **not** | `git commit --only` fired the consumer repo's `pre-commit` hook, which on benoticed.co stashes unrelated dirty files — a crash mid-hook loses the human's work silently. State commits are machine-authored and scope-restricted to non-source paths (`isStatePath`), so a host lint/test hook has nothing to validate here, while its stash/restore step can destroy unrelated dirty work and a tree-mutating hook (formatters) can desync the index from the txn marker. `stateCommit`'s own `unrelated-dirty` refusal is the real gate on these commits. Ship's commit touches real source, so the host gate belongs there. Parallel flows multiply state-commit frequency, so the exposure scales with 74 |
| 79 ✽ | Implement dispatch budget | `implement.stepsPerDispatch` (config, integer ≥1, default **3**): the implement session works each slice of **at most N of the plan's remaining steps** and relays — `/clear`, then re-entry (82 ✺) — only at **evidence-cycle boundaries**, never mid-red. `start` prints `dispatch-budget:` and the arithmetic (`steps: 18 ≈ 6 dispatches`) so the skill reads N mechanically; the **plan gate** prints the same arithmetic line, so the human approving the plan sees the run's shape before it starts (display only — no lens, no threshold, no calibration surface). **Early-exit protocol** replaces vibes with countable triggers: a slice that exceeds ~15 inner-loop iterations inside one step, or leaves the step's scope to fight infrastructure, finishes the red→green it is in, reports the excursion, and relays early. Enforcement is skill-tier by design — this is economics, not evidence; an overrun fakes nothing, it only costs money, so no witness machinery is owed. Operator notes (non-load-bearing): `CLAUDE_CODE_AUTO_COMPACT_WINDOW` at launch as belt-and-suspenders (unverified); keep the host awake — a suspend >1h expires the prompt cache and repays the full context at write price on wake | d7eb5a85 (benoticed booking-services, 2026-07-19→21): the only relay trigger was agent judgment, which arrived at ~500k tokens of context — 631 requests for four steps, three agents averaging ~510k context/request, 88% of 756M cache-read tokens, every request above 200k billed at the doubled long-context tier. The relay mechanism itself proved *sound* — worktree + plan + journal carried everything, and the "continue at s5" agent resumed cleanly — only the trigger was undesigned. Steps are the one unit both the skill and the journal can count; token counts are invisible to the agent itself (the vibes that produced the incident). Autocompaction was grilled as the alternative and rejected as substitute: it triggers at the *window* (sonnet-5's is a fixed 1M — never at the price boundary; the default threshold sits ~967k, which is why it never fired), it swaps witnessed disk state for a model-written summary as the agent's entire memory (the relaying-agent adversary of 48, one layer down), it fires at unchosen moments (mid-excursion, where relay is forbidden), and whether a subagent compacts or dies at the window is undocumented. **Compaction is the harness's continuity mechanism, not specflow's economics mechanism.** The budget bounds routine runs; the early-exit protocol bounds the pathological one (aeb's infra firefight — the thing that actually built the 500k context — was emergent early-exit behavior arriving late; this codifies it with an earlier trigger). N=1 would repeat the ~50–100k warmup per step for nothing; 3 keeps the back half of a browser-e2e run under the long-context tier. Grill #9 (82 ✺) moved the dispatch surface from Task subagents to session slices; the budget, the evidence-cycle boundary, and the early-exit triggers are unchanged — c270d08b re-validated the host-awake note twice (two lid-close suspends froze a live slice mid-run) |
| 80 ✽ | Implement loop width | Three protocol lines in the implement skill, no CLI surface: **(1)** the inner TDD loop runs **exactly the test under work** (`-t` with the test's own name, never the spec tag) — rerunning a failing test N times is legal, rerunning the *suite* to watch one test is not; **(2)** the tagged suite runs only **inside the witnessed evidence cycle** at step close — "check nothing broke mid-step" duplicates a run the CLI already witnesses, for free; **(3)** fat artifacts (the living design HTML, long plan docs) are read **by section** — anchor-grep, then offset reads of the ranges you need — never whole into the implement session's context. Plus one D66 guidance line: in browser-e2e repos, keep the app server alive across the inner loop where the harness allows — the per-run floor is boot + compile, not test time | d7eb5a85: 194 test invocations at suite width (`-t "@spec:booking-services"`), 2–3.5min each, ~3h of test wall-time — repetition was legitimate TDD, width was pure waste, and row 137's filtered-template design already distinguishes the levels; the loop just never used the narrow end. Suite dumps were also a fat slice of what built the ~500k contexts, so width feeds directly into 79's budget math — narrower runs make N=3 more comfortable, not less. The sectioned-read line inherits the reader-agent win (the s5 agent dispatched three sonnet readers to digest the 34-frame design HTML for ~6M cache-read instead of carrying 100k+ of markup for hundreds of requests) minus the dispatch: grill #9's no-subagent rule (82 ✺) retires the reader surface, and anchor-targeted reads keep the same economics — pay for the frames you need, once. Enforcement skill-tier for the same reason as 79: economics, not evidence |
| 81 ✽ | Dispatch telemetry | New journal entry kind `dispatch` — CLI-written when the skill reports a completed session slice (82 ✺): `{plan, ordinal, steps_assigned, steps_completed, usage: {tokens, tool_uses, duration_ms}, reported: true}`, ordinal derived by the CLI from prior `dispatch` entries in the plan's stream. Explicitly **telemetry, not evidence**: the numbers are model-reported (the slice's own usage surface; `tokens` omitted where the surface doesn't expose it), labeled so on the entry, and **no gate may ever consume a `dispatch` entry** — telemetry in, decisions never out. No aggregation surface; queries stay `jq` over the journal until real pain | this grill exists because answering "why did that run cost so much" required transcript archaeology in `~/.claude` internals — deduping streamed usage rows by message id in a directory specflow doesn't own, under a retention policy it doesn't control. The Agent-tool return already hands the orchestrator usage numbers that currently evaporate. With the entry, `dispatches-per-plan` and `tokens-per-step` become journal queries, and the dispatch budget (79) gets its own regression signal — budget drifting up means repo test cost or plan quality is degrading. Admissible against the witnessed-spine doctrine because the journal already carries diagnostic metadata that isn't evidence (model ids, plugin versions, `calibration: local` stamps); reported usage is the same genre, one entry kind over — the fence ("no gate consumes it") is what keeps the evidence/telemetry line sharp |
| 82 ✺ | Session relay | Implement and ship execute in **fresh interactive sessions the human spawns**, never Task-tool subagents — the subagent leaves specflow's execution model everywhere (the one spawn surface left is the CLI's own headless reviewer battery, which is the judgment lane (48), not a dispatch). `next` emits a **`home:`** field with each stage row — the plan's worktree for implement, the primary root for ship. Engine rule: `home:` ≠ cwd → print the handoff line verbatim — `cd <home> && claude --model <exact pin> "/specflow"`, `--manual` appended when the session is armed — and END TURN; `home:` = cwd → invoke the stage skill here. The implement session **is** the implementer: same TDD protocol, same witnessed evidence, gate stops render in the terminal the human is already watching, and the `gates.implement.model` pin rides `claude --model` literally — the "map to your dispatch surface's nearest knob" fudge dies. Intra-plan relay: after each ≤`stepsPerDispatch` slice the skill reports telemetry (81), prints `run /clear, then /specflow`, and ends its turn; the cleared session re-derives position from the journal — re-entrancy (32) becomes the routine per-slice mechanism, not just the crash path. Handoff emission is idempotent: `next` from the wrong cwd re-prints the same line. Build note: verify `next` resolves state from a worktree cwd (gate, test-evidence and log already do) | c270d08b measured the subagent tax three ways: an idle orchestrator (188 opus requests at ~308k average context, 58M cache-read, spent relaying notifications and spawning), an invisible worker (dispatch results queued into a session nobody watched — "what next i didn't get"), and unkillable waste (a 6.4h-wall suspended dispatch holding CPU while the evidence suite starved past its timeout; a predecessor's dead dispatch costing 1.7h of inheritance forensics). A fresh session is the same model at main-loop speed, watched, steerable, and disposable — dead means paste the command again, the journal carries position. A printed command is also trivially runnable on a remote box: the precondition for deleting the run's actual majority cost (~21h of lid-closed/away wall), which no local design can fix. Fat-artifact digestion moves from reader agents to sectioned reads (80) |
| 83 ✺ | Reviewer policy pins | `decide implement <plan-id> --pin "<policy>"` (composable with `--revise` and `--approve`) appends journal kind **`policy-pin`** — a human-authored content policy, plan-scoped, append-only; a later pin supersedes its predecessor on the same subject, and upstream graduation (`--revise --upstream`) is how a policy becomes spec truth. The CLI injects all live pins into **every subsequent reviewer battery** for the plan — code lenses and the vision design-reviewer alike — as settled constraints (do not re-litigate; a finding that requires violating a pin must cite it), and pins join the verdict-cache key exactly like injected docs (68): a changed pin-set re-arms review. Detection is two-layer because deterministic semantic matching of findings against pins is not honestly implementable: the verdict schema gains an optional **`contradicts_pin`** field the reviewer self-reports and the CLI escalates to a standing stop; and the decide rendering always lists live pins beside the findings, so a silent contradiction is caught by the human in the same glance, not a round later. Pins are decisions, not telemetry — gates consume them, same standing as `human-decision` entries | c270d08b's costliest single defect wasn't code: r1's correctness lens forced stripping every service field from the unavailable `/book` page (draft-must-not-leak), r2's design lens demanded the page render the service in full, and neither lens could see the other's ruling — batteries are stateless per content sha by design (75 re-arms on change, 76 runs them outside the lock). Settling the contradiction took `--revise --upstream`, a plan reopen, and the overnight round (~8h wall). Pins are the minimal cross-round memory: born exactly where the human already stands when a policy dispute surfaces (the decide prompt), journaled so they can't drift or be silently edited, injected by the CLI that already owns reviewer invocation (48), and checked without trusting reviewer cooperation. The escalation is mechanical where it can be (`contradicts_pin` → standing stop) and human-visible where it can't (pins rendered at every decide) |

| 84 ✾ | Abandon's revert boundary is the effort's own write | `planItems` takes an explicit **`Pairing`** rather than inferring one. `'pair-parent'` (`abandon <plan-id>`) unwinds exactly the amendment this plan was paired with, pinned on the plan's own `derives-from`, and refuses `stacked-amendment` if the spec has moved since. `'stamp-only'` (`abandon <effort-slug>`) reverts nothing here — the effort path's own spec loop is the sole revert, pinned on **that effort's write sha**. The two paths legitimately need different pins, and that is the design, not a wart. Consequence: an effort-path item is now exactly one artifact, so the `N artifacts walked` line a human reads is accurate | `abandon(ratchet-scope)` destroyed an approved amendment owned by `error-class-ownership`, silently. Ownership was re-derived through `effortOf(planId)`, which tie-breaks **alphabetically** across every effort that ever wrote the plan — so a plan rewritten by a later effort resolved to a *sibling* effort whose writes did include the parent spec, and the revert walked that spec back past the abandoned effort's own write boundary. Two efforts touching one plan is the normal case once a chore corrects a feature's plan, so the alphabetical tie-break was load-bearing by accident. The effort path already had the correct mechanism; routing through `planItems` gave it a second, wrong one, and `revertedSpecs` existed only to dedupe the two. Cost: two full decompose rounds (round 1's six blocking findings were artifacts of reverted content) and `specflow check` reported `0 errors` before and after. **Deliberately not fixed:** `check` still cannot see this — after a *legitimate* revert a spec correctly no longer matches its newest journal write, so the invariant needs a revert-boundary concept before it can avoid false positives |
| 85 ✾ | `next` names an effort or asks for one — never a placeholder | Plan-authoring routes resolve the slug through **`liveOwner`**: a live effort that wrote this plan, else one that wrote its parent spec. With no candidate the owed work is not the plan but an effort, so the line becomes `specflow recap` at **`stage: brainstorm`** — the stage rides with the line, because a caller keeping `stage: 'plan'` would hand the plan skill a recap command to run. `effortOf` is deliberately **not** reused for routing: it answers with an *abandoned* effort whenever that is the only writer, and a write must never be booked onto a terminal stream. A planless spec whose write can be booked **outranks** one needing a recap first, so the fallback cannot stall runnable work behind it | `next` printed the literal `specflow write <plan-id> --effort <slug>` — a command that cannot be run. After `abandon <effort>` there was nothing to substitute either: the authoring effort is terminal and filtered out of the live list, while its parent spec stays `approved` with no live plan and therefore reads as planless on every subsequent turn. The loop was total — `next` repeated the same dead line indefinitely, and the only escape was to drive `specflow start` by hand. Falling through instead of routing would have been no better: the tail of `computeNext` is `specflow check`, which reports zero errors and sends the loop straight back. A placeholder is only honest where the CLI genuinely cannot know the answer (`<spec-id>`, `<plan-id>` — the human picks); the slug was always derivable, which is what made this a bug rather than a prompt |
| 86 ✾ | A chore never reaches the decompose stage | `next` reads the effort's class and routes a chore straight to plan work; the decompose stage is skipped entirely, not merely its gate. The chore's plan parent — the spec whose implementation area it touches, else `principles` — is therefore chosen at **plan** authoring, and `specflow-plan`'s SKILL owns that rule | The decompose stage was unsatisfiable in both directions for a chore: `next` routed to `gate decompose`, which refuses `nothing-to-gate` without written specs, while `write` refuses spec content from a chore as a `class-tripwire` **by definition of the class**. Neither side could ever be satisfied, so every chore effort deadlocked at the stage and the only way through was to skip it by hand and gate the chore's plans directly. Skipping only the gate was considered and rejected: the decompose skill's chore path wrote nothing, so the stage had no command for `next` to emit and would have re-routed to the same zero-writes state forever — a stage whose skill produces no CLI motion cannot be a `next` destination. That move left `specflow-decompose`'s chore paragraph asserting *"Decompose shrinks to routing; it never skips"*, which was then false; the parent-choice rule moved to `specflow-plan` and a test pins both halves so the docs cannot drift back || 87 ❂ | Harness-independence is a property of the CLI, not of the prose | specflow drives from any harness the [skills](https://github.com/vercel-labs/skills) installer reaches, with **Claude Code and Pi supported** (engine + guard + dashboard) and the rest **skills-only**. Three mechanisms: **(1)** every harness-shaped string moves out of the six skills and `commands/specflow.md` into the CLI — `next` renders `run:`/`relay:` from a typed per-harness registry constant, and a **neutrality test** over `plugin/skills/**` *and* `plugin/commands/**` fails the build if a relay command (`/clear`, `/new`), a harness tool name (`MultiEdit`), `PreToolUse`, `subagent` or `Invoke skill` appears. The rule is that *commands and tool names* are CLI-supplied; harness **names** stay legal in prose, because README needs them and a regex banning "Pi" is unimplementable. **(2)** The guard's logic becomes a dependency-free `plugin/hooks/canon-guard.mjs` with thin adapters: `guard-state.mjs` (stdin JSON → exit 2) and a Pi extension (`tool_call` → `{block, reason}`), both importing it by **relative path** — not as a package export, because the Claude hook runs from `${CLAUDE_PLUGIN_ROOT}` (no `node_modules`) and a Pi user's repo has no specflow install, so a package import resolve-fails into fail-open, i.e. a *silently absent* guard. Both adapters ship as **siblings of the core in the source tree too** (`plugin/hooks/specflow-pi.ts`), so the one relative specifier is valid in the repo and in both installed layouts; a specifier that only resolves post-install is a trap no unit test can hold. **(3)** `specflow init --agent <name>` **syncs and commits** the payload set the installer cannot place; committing is load-bearing, because pi resolves project extensions and prompts cwd-relative with no upward walk and only committed files reach `.specflow/worktrees/<plan-id>`. Sync, not install-once: the engine file's `npx -y @whatmatters/specflow@<v>` pin is the single point deciding which CLI version the entire pipeline runs, so an install-once rule pinned every repo to whatever version first touched it — forever, silently, while re-running init printed `already installed`. Absent → write; identical → skip; differing **only** by pin → restamp; differing otherwise → leave it and report `payload-modified`. Targets are `git check-ignore`d *before* any write, because the only honest answer to a gitignored payload path is a refusal: `git add -f` overrides a rule the human wrote down, and writing-without-committing gives the primary root a guard and the worktree none. Detection is the authority (`SPECFLOW_HARNESS` → `PI_CODING_AGENT` → `CLAUDECODE` → `harness:` → `claude-code`), tested on presence, and resolution deliberately does **not** sit in `loadConfig` — an unread key must not brick `specflow check`. Pi's guard needed one thing Claude Code's did not: pi **blocks** the tool when a `tool_call` handler throws ("fail-safe", `docs/extensions.md`), the inverse of a hook exiting non-zero, so the adapter's `try` is what preserves row 31 there — an unparseable config would otherwise refuse every write in the repo, `src/` included. Four residuals are accepted, not fixed: the **reviewer lane is Claude on every harness** (`reviewer.ts:113` spawns `claude -p`; the calibration matrix, model floor, prompts and verdict-shape contract are all built on it, so a swappable reviewer would make "calibrated" meaningless); **calibration stays Claude-Code-only**; **skills must be installed at global scope**; and — a consequence of the first — **`gates.<stage>.model` means two things on Pi**, the Anthropic model the reviewers run and the provider-qualified model the Pi worker session runs, so a non-Anthropic implement worker is not expressible. That last one is also why there is no `provider:` config key: with the pin forced to a claude-runnable id, `anthropic` is a consequence rather than a preference, and the only other reachable state was a runnable-*looking* line that resolves to nothing | Measured coupling at 0.2.2 was one harness-shaped string in `src/` (`handoffLine`, `next.ts:71`) plus ~15 lines of prose — grill #9 (row 82) had already made the driving session harness-independent by retiring Task subagents and moving evidence-witnessing into the CLI. The first draft's design — per-harness *rendering* from a placeholder vocabulary, plus a specflow-owned `pi/` tree and a second distribution surface — was killed by two findings: the installer's default is a **symlink to one canonical copy**, so rendered variants have nowhere to live; and the harness-specific prose turned out to be CLI-supplied information wearing a costume (the relay command is already printed by `next`, the guard's refusal already names the tool it blocked in the harness's own vocabulary, and row 31 already says the hook is friction while the trailer audit is the guarantee). Keeping it would have meant owning a template layer and golden-file tests to reach two harnesses, where the ecosystem reaches 74 with none of it. Two corrections came from reading the installed artifacts rather than the docs: Pi has **no skill-invocation tool** (skills arrive as a system-prompt block naming each `location` under "use the read tool to load a skill's file"), so `Invoke skill X` was a Claude-Code-only phrasing hiding in a row the first revision had marked "unchanged"; and Pi's **default provider is `google`**, so a bare `--model claude-opus-5` in a Pi handoff resolves wrong or not at all — which is why the model flag is a renderer and not a string, and why an uncalibrated relay session was a live risk to what row 83's pins rest on. Dogfooded end to end from Pi 2026-07-30/31 — one complete effort (`cart-discounts`) driven brainstorm → merged PR on `KostiantynPopovych/specflow-pi-dogfood#1` (squash `bb8c146`), journal streams `cart-discounts` (13 entries) and `cart-discounts-plan-1` (17), final `check` 0 errors over 28 audited commits, spec `live` / plan `done` via the lazy stamp. pi 0.83.0 authored every artifact, `claude -p` ran every reviewer, a human took every `decide`. The run earned its keep: the decompose gate caught an **unreachable criterion** (`ac-floor` could not fire under a 0–100% discount of a non-negative subtotal), then caught the **contradiction the fix introduced** (`percent=0` on a negative subtotal), then correctly escalated the resulting input-contract widening as a *scope* question for the human — and the round bound forced `--approve --override` rather than letting a third round pass quietly. `verify-red` reconstructed a genuine red after pi front-ran a later step's clamp, which `test-evidence` had already refused as `vacuous`. Two Tier-1-invisible costs: `pi -p` **stalls on long prompts** (terse + `--thinking off` is reliable, multi-paragraph is not), and pi **reformatted pre-existing code**, which three separate reviewers flagged as churn. Execution found four defects the plan had not: toon's `esc()` quotes any value containing a comma, so the planned `relay: /clear, then /specflow` would have rendered quoted and been printed quoted by the skill that must print it verbatim (the repo's own convention — `·` separators everywhere in `check`/`dispatch` — already encoded the rule); the pi adapter's relative import of the core resolved only post-install; a `payload-stale` regex that stopped at whitespace/quote/paren swallowed the `}` in `${SPECFLOW_BIN:-npx -y @whatmatters/specflow@<v>}` and so fired on **every** fresh install; and restamping an *uncommitted* pin edit restores the file to its HEAD content, leaving `git commit --only` nothing to commit and throwing out of `gitio` — now a legal silent success, the same rule as nothing-to-write |
| 88 ✹ | The judgment lane runs on the resolved harness | Overturns row 87's four residuals: `invokeReviewer(ctx, harness, opts)` spawns the RESOLVED harness's headless mode (claude-code: `claude -p --output-format json`; pi: hermetic `pi -p --mode json --no-session --no-extensions --no-skills --no-context-files --thinking <level>` — every omitted flag was a machine-local variable silently changing reviewer behavior, and this machine's `defaultThinkingLevel: xhigh` was the probable true cause of row 87's "stalls on long prompts"). Full routing, NO fallback: a silent claude fallback would swap the reviewer identity mid-pipeline. The pin grammar becomes `[provider/]model[:thinking]` — one knob (row 87's no-`provider:`-key argument dies with the forced-claude pin that justified it); bare pins resolve through the harness default provider, provider-qualified pins refuse `provider-unrunnable` on claude-code, omitted thinking is `off` on every harness, and claude-code renders non-off levels through a pinned `MAX_THINKING_TOKENS` budget table. The verdict-cache key and gate-run entries gain `harness` (absent reads `claude-code` — a pi verdict must never cache-hit a claude one); calibration becomes per-(harness, model): the shipped matrix stays claude-code, `specflow calibrate` routes through the harness and writes `matrices.<name>.models` to the local overlay, and an uncalibrated (harness, model) pair rides the EXISTING floor machinery — loud warning, `--manual` stop, `calibration: local` stamps. `check` probes the resolved harness's launch binary. Both other reviewer lanes follow the same rule: `check --drift --deep` resolves the harness, and calibrate's implement-seed WORKER spawns `harness.worker` (pi needs no permission-bypass flag — its built-in tools are not approval-gated, and `--approve` governs project-local file trust, a different axis). Residual accepted, not fixed: Anthropic structurally privileges `claude -p` (first-party, plan limits) over third-party harnesses — the empirical probe hit `400 "Third-party apps now draw from your extra usage"` on a fresh `pi -p` spawn against subscription OAuth while `claude -p` answered in 3.6s on the same machine; under full routing that surfaces as a loud in-stream `reviewer-invocation` refusal (pi's `--mode json` reports provider errors as `stopReason: "error"` with exit 0 — the envelope parser, not the exit code, is the error channel), and the remedy is the user's provider choice, not specflow's | Row 87 measured the reviewer lane as the deepest coupling and accepted it four ways (claude-on-every-harness, claude-only calibration, dual-meaning `gates.model`, no provider key); the 2026-07-31 design interview overturned it after a probe re-tested the two operational objections and found both soft — the "long-prompt stall" resolved into closed upstream stdin/loop bugs (earendil-works/pi #161 #2584 #2677 #4303 #5571) compounded by the xhigh default, and headless PNG input is a documented pi feature (`pi -p @screenshot.png` file arguments; the Read tool sends images as attachments) — while the billing asymmetry hardened from anecdote to a reproduced 400. Chosen over two rejected shapes: silent-fallback routing (corrupts the cache key and the calibration claim) and reviewer-lane-stays-claude with an opt-in knob (preserves the dual-meaning pin and the claude-on-PATH requirement for pure-pi users — the two residuals the redesign existed to kill) |

---

## Open / deferred

- **The skills battery cannot detect a small regression in the `decompose` row.** Measured 2026-07-30 while re-validating row 87's prose edit: at `calibrate`'s default `samples: 10`, one *fixed* input produced 5/10, 7/10 and 9/10 across three independent runs, while the same input elsewhere produced three identical 6/10s. So a two-run control can agree exactly and still fail to bound the noise — clustering reads as determinism. `plan` (10/10 everywhere) and `implement` (0/3 everywhere) are stable; `decompose` is not. Consequence: any workflow step of the form "edit the skill prose, re-run the battery, compare" will generate false signals on that row, and acting on them means editing prose until the sampler cooperates. Fixing it needs materially more samples (linear in real `claude -p` cost) or a determinism lever `calibrate` does not expose — no seed, no temperature pin. Left open; until then the honest verdict for a prose edit on that row is *inconclusive*, not *unchanged*.
- **Skills installed from a version-pinned tarball URL do not auto-update.** npm serves no `latest.tgz` alias, so `skills update` cannot resolve forward; users re-run `add` with the new version URL. The obvious mitigation does not work: each skill pins `npx -y @whatmatters/specflow@<v>` (stamped by `scripts/sync-versions.mjs`), so the CLI that runs is *always* the version the stale skills chose, and `dashboard.ts:51` prints that version and sees nothing wrong. Detecting skew needs a registry query. Left open. **Narrowed** by row 87's payload sync: the *payload* half of that skew is now detected (`check` → `payload-stale`) and fixed (`init --agent <name>` restamps), because those files live in the repo where the CLI can read them. The *skills* half still cannot be seen from inside a repo.
- **`gates.<stage>.model` is one key with two consumers.** It pins the stage's gate reviewers (always Claude, decision 12) and the implement session's own model, which on Pi is a different program. Forcing them equal is what keeps rows 79–81's calibration story coherent, and splitting them (`implement.agentModel`) is the fix if anyone actually wants a non-Anthropic implement worker. Deferred to real demand; parity is the bar, not exploitation.
- **A partially-supported agent tier exists.** Agents other than Claude Code and Pi get the six skills with no guard and no engine. Honest, since the trailer audit is the actual guarantee (row 31) — but the docs must say which agents are which rather than implying 74 supported harnesses.
- **`skills` and `pi` are both unpinnable dependencies.** Tier 1 never loads either, so an upstream change is invisible until the next Tier 2 dogfood. Mitigations chosen: the version note in README + row 87, and the fail-open guard. Accepted residual.
- **Skill-name collisions when a Claude Code user installs both paths.** Pi's loader logs a `collision` diagnostic and keeps the first (`skills.js`, `loadSkills()`); Claude Code's behavior with a plugin skill and an ecosystem skill of the same name is unverified. Only bites someone running the plugin *and* `skills add` for the same harness — cheap to document once known.
- **A plugin install and a project-scope install both fire.** Verified 2026-07-30: on a machine carrying the marketplace plugin, `init --agent claude-code` in a repo means a canon write is blocked twice with the identical sentence. Harmless (the verdict is the same and the block is idempotent), but it makes the two paths indistinguishable in output — any future probe of the project-scope path must mark the copies before trusting the result.
- **Bash-matching parity.** The guard's Bash heuristic is best-effort in Claude Code (row 31) and stays best-effort elsewhere; whether Pi's `user_bash` event should also be guarded is deferred to real pain.
- **Which real repo validates the tool** — resolved by BUILD-PLAN (2026-07-08): **dogfood on specflow itself from Slice 2** — the CLI repo has tests from day one (the protocol tier *is* the harness), a real change stream (Slices 3–4), lowest stakes. `benoticed.co` is the graduation target once the pipeline survives its own build. (Ground truth from the 2026-07-08 survey: only `benoticed.co` is under active development; no candidate repo had tests or CI.)
- ~~**Parallel execution** (drain-the-DAG waves) — deferred~~ — **resolved by grill #7 (rows 73–78, 2026-07-18)**: several flows now run concurrently in one repo. Code stays isolated per-plan in worktrees, canon and journal stay shared on the primary root, and the three changes that make it safe are a read path that advances in-flight work first (74), a contention path that stops holding the global lock across LLM batteries (76), and a merge path that rebases before it reviews (77) — with staleness solved once, by approval lapsing on content change (75), rather than three times.
- **Protected-main team repos** — state commits would need to travel via PR; deferred until the tool targets teams. Same bucket: one user on two machines (single-writer holds per-clone; a rejected push is the detection signal).
- **Re-adding `CONTEXT.md` / `adr/`** — only on real pain (term-drift; needing superseded decisions).
- **Ephemeral brainstorm scratch-dump** — only on real pain, now narrowed to interviews dying **mid-interview, before recap confirmation**; a confirmed recap already survives in the effort journal.
- **Multiple criteria runners per repo** (monorepos with separate suites) — deferred with `ship.test`'s singular shape; one runner *string* per repo until real pain. Report side already solved (glob-merge, 63); **graduation precondition**: verify filtered + full-suite modes on benoticed.co (`pnpm -r`, 20 vitest packages) before adopting.
- **Per-criterion flake `retries:`** — deferred; the drift debounce (Decision 53) is the first line, retries only if debounce proves insufficient on a real suite.
- **Out-of-band design drift** — a `check --drift --deep` design lane that re-judges live `ui` specs' screens against their canon between ship gates (the design twin of Decision 42's deterministic drift). Deferred to real pain: the implement lens catches design regressions on every UI plan, and out-of-band screen rot (a hotfix that uglifies a shipped screen) has no reported incident yet.
- **Design-stage / main-session economics** — grill #8 priced only the implement run. **Main-session half resolved by grill #9 (row 82, 2026-07-22)**: the engine session no longer holds context while implement or ship runs — those stages execute in their own sessions and the engine's turn ends at the handoff (c270d08b's engine: 188 opus requests at ~308k average context, 58M cache-read, mostly babysitting dispatches). The **design-stage session** (browser-driven, d7eb5a85: part of 235 opus requests at ~365k average context, 85.7M cache-read) remains unexamined. Future grill candidate; no incident beyond cost yet.
